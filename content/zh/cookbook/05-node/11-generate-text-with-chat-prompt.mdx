---
title: "使用聊天提示词生成文本"
description: "了解如何使用 AI SDK 和 Node 通过聊天提示词生成文本"
tags: ['node', 'chat']
---

# 使用聊天提示词生成文本

之前，我们能够使用单条消息提示词、系统提示词或两者的组合来生成文本和对象。然而，有时您可能希望基于一系列消息来生成文本。

聊天补全允许您基于一系列消息生成文本。这一系列消息可以是任意数量的系统之间的任意交互序列，但最流行且最相关的用例是代表用户与模型之间对话的一系列消息。

```ts file='index.ts'
import { generateText } from 'ai';

const result = await generateText({
  model: 'openai/gpt-4o',
  maxOutputTokens: 1024,
  instructions: 'You are a helpful chatbot.',
  messages: [
    {
      role: 'user',
      content: [{ type: 'text', text: 'Hello!' }],
    },
    {
      role: 'assistant',
      content: [{ type: 'text', text: 'Hello! How can I help you today?' }],
    },
    {
      role: 'user',
      content: [{ type: 'text', text: 'I need help with my computer.' }],
    },
  ],
});

console.log(result.text);
```
