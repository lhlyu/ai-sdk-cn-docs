---
title: 流式对象
description: 了解如何使用 AI SDK 和 Node 流式传输结构化数据
tags: ['node', '流式传输', '结构化数据']
---

# 流式对象

对象生成有时可能需要很长时间才能完成，
尤其是在生成大型模式时。

在生成式 UI 用例中，将对象实时流式传输到客户端非常有用，
以便在对象生成过程中渲染 UI。
你可以使用带有 `Output.object` 的 `streamText` 来生成部分对象流。

```ts file='index.ts'
import { streamText, Output } from 'ai';
import { z } from 'zod';

const { partialOutputStream } = streamText({
  model: 'openai/gpt-4.1',
  output: Output.object({
    schema: z.object({
      recipe: z.object({
        name: z.string(),
        ingredients: z.array(z.string()),
        steps: z.array(z.string()),
      }),
    }),
  }),
  prompt: '生成一份千层面食谱。',
});

for await (const partialObject of partialOutputStream) {
  console.clear();
  console.log(partialObject);
}
```
