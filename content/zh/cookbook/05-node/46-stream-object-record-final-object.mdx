---
title: 流式传输对象后记录最终对象
description: 了解如何使用 AI SDK 和 Node 在流式传输对象后记录最终对象
tags: ['node', '流式传输', '结构化数据']
---

# 流式传输对象后记录最终对象

当您流式传输结构化数据时，可能需要记录最终对象以用于日志记录或其他目的。

## `onEnd` 和 `output`

使用 `onEnd` 获取元数据（如 token 用量），并等待 `result.output` 获取最终的结构化对象。

```ts file='index.ts' highlight={"15-29"}
import { streamText, Output } from 'ai';
import { z } from 'zod';

const result = streamText({
  model: 'openai/gpt-4.1',
  output: Output.object({
    schema: z.object({
      recipe: z.object({
        name: z.string(),
        ingredients: z.array(z.string()),
        steps: z.array(z.string()),
      }),
    }),
  }),
  prompt: 'Generate a lasagna recipe.',
  onEnd({ usage }) {
    console.log('Token usage:', usage);
  },
});

for await (const _ of result.partialOutputStream) {
}

try {
  const output = await result.output;
  console.log('Final object:', JSON.stringify(output, null, 2));
} catch (error) {
  console.error('Failed to parse output:', error);
}
```

## `output` Promise

`streamText` 结果包含一个 `output` Promise，它会解析为最终对象。
该对象具有完整的类型。当根据 schema 的类型验证失败时，Promise 会被拒绝并抛出 `TypeValidationError`。

```ts file='index.ts' highlight={"17-26"}
import { streamText, Output } from 'ai';
import { z } from 'zod';

const result = streamText({
  model: 'openai/gpt-4.1',
  output: Output.object({
    schema: z.object({
      recipe: z.object({
        name: z.string(),
        ingredients: z.array(z.string()),
        steps: z.array(z.string()),
      }),
    }),
  }),
  prompt: 'Generate a lasagna recipe.',
});

for await (const partialObject of result.partialOutputStream) {
}

try {
  const { recipe } = await result.output;
  console.log('Recipe:', JSON.stringify(recipe, null, 2));
} catch (error) {
  console.error(error);
}
```
