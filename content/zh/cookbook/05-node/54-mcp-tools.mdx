---
title: 模型上下文协议 (MCP) 工具
description: 学习如何在 AI SDK 和 Node 中使用 MCP 工具
tags: ['node', '工具使用', '智能体', 'mcp']
---

# MCP 工具

AI SDK 通过提供一个轻量级客户端来支持模型上下文协议 (MCP) 工具，该客户端暴露了一个 `tools` 方法，用于从 MCP 服务器获取工具。使用后，应始终关闭客户端以释放资源。

如果你更倾向于使用官方传输层（可选），请安装官方模型上下文协议 TypeScript SDK。

<Snippet text="pnpm install @modelcontextprotocol/sdk" />

```ts
import { createMCPClient } from '@ai-sdk/mcp';
import { generateText, isStepCount } from 'ai';
import { Experimental_StdioMCPTransport } from '@ai-sdk/mcp/mcp-stdio';
import { openai } from '@ai-sdk/openai';
// 可选：如果你更喜欢官方传输层
// import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio';
// import { SSEClientTransport } from '@modelcontextprotocol/sdk/client/sse';
// import { StreamableHTTPClientTransport } from '@modelcontextprotocol/sdk/client/streamableHttp';

let clientOne;
let clientTwo;
let clientThree;

try {
  // 初始化一个 MCP 客户端以连接到 `stdio` MCP 服务器（仅限本地）：
  const transport = new Experimental_StdioMCPTransport({
    command: 'node',
    args: ['src/stdio/dist/server.js'],
  });

  const clientOne = await createMCPClient({
    transport,
  });

  // 通过客户端传输配置直接连接到 HTTP MCP 服务器
  const clientTwo = await createMCPClient({
    transport: {
      type: 'http',
      url: 'http://localhost:3000/mcp',

      // 可选：配置请求头
      // headers: { Authorization: 'Bearer my-api-key' },

      // 可选：提供 OAuth 客户端提供程序以进行自动授权
      // authProvider: myOAuthClientProvider,
    },
  });

  // 通过客户端传输配置直接连接到服务器发送事件 (SSE) MCP 服务器
  const clientThree = await createMCPClient({
    transport: {
      type: 'sse',
      url: 'http://localhost:3000/sse',

      // 可选：配置请求头
      // headers: { Authorization: 'Bearer my-api-key' },

      // 可选：提供 OAuth 客户端提供程序以进行自动授权
      // authProvider: myOAuthClientProvider,
    },
  });

  // 或者，你可以使用官方 SDK 创建传输层，而不是直接配置：
  // const httpTransport = new StreamableHTTPClientTransport(new URL('http://localhost:3000/mcp'));
  // clientTwo = await createMCPClient({ transport: httpTransport });
  // const sseTransport = new SSEClientTransport(new URL('http://localhost:3000/sse'));
  // clientThree = await createMCPClient({ transport: sseTransport });

  const toolSetOne = await clientOne.tools();
  const toolSetTwo = await clientTwo.tools();
  const toolSetThree = await clientThree.tools();
  const tools = {
    ...toolSetOne,
    ...toolSetTwo,
    ...toolSetThree, // 注意：此方法会导致后续工具集覆盖同名的工具
  };

  const response = await generateText({
    model: 'openai/gpt-4o',
    tools,
    stopWhen: isStepCount(5),
    messages: [
      {
        role: 'user',
        content: [{ type: 'text', text: '查找价格低于 100 美元的产品' }],
      },
    ],
  });

  console.log(response.text);
} catch (error) {
  console.error(error);
} finally {
  await Promise.all([
    clientOne.close(),
    clientTwo.close(),
    clientThree.close(),
  ]);
}
```
