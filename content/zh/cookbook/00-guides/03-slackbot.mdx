---
title: "Slackbot 智能体指南"
description: "了解如何使用 AI SDK 在 Slack 中构建 AI 智能体。"
tags: ['agents', 'chatbot']
---

# 使用 AI SDK 在 Slack 中构建 AI 智能体

在本指南中，您将学习如何使用 AI SDK 构建一个由 AI 驱动的 Slackbot。该机器人将能够利用线程的完整上下文，响应私信和频道中的提及。

## Slack 应用设置

在开始构建之前，您需要创建并配置一个 Slack 应用：

1. 前往 [api.slack.com/apps](https://api.slack.com/apps)
2. 点击“创建新应用”并选择“从头开始”
3. 为您的应用命名并选择您的工作区
4. 在“OAuth 与权限”下，添加以下机器人令牌作用域：
   - `app_mentions:read`
   - `chat:write`
   - `im:history`
   - `im:write`
   - `assistant:write`
5. 将应用安装到您的工作区（“OAuth 令牌”部分下的按钮）
6. 复制机器人用户 OAuth 令牌和签名密钥，供下一步使用
7. 在 App Home -> 显示选项卡 -> 聊天选项卡下，勾选“允许用户从聊天选项卡发送斜杠命令和消息”

## 项目设置

本项目使用以下技术栈：

- [Vercel 的 AI SDK](/docs)
- [Slack Web API](https://api.slack.com/web)
- [Vercel](https://vercel.com)
- [OpenAI](https://openai.com)

## 快速开始

1. 克隆[该仓库](https://github.com/vercel-labs/ai-sdk-slackbot)并检出 `starter` 分支

<Snippet
  text={[
    'git clone https://github.com/vercel-labs/ai-sdk-slackbot.git',
    'cd ai-sdk-slackbot',
    'git checkout starter',
  ]}
/>

2. 安装依赖

<Snippet text={['pnpm install']} />

## 项目结构

starter 仓库已包含：

- Slack 工具函数（`lib/slack-utils.ts`），包括验证传入请求、将 Slack 线程转换为 AI SDK 兼容的消息格式，以及获取 Slackbot 的用户 ID
- 通用工具函数（`lib/utils.ts`），包括初始 Exa 设置
- 处理不同类型 Slack 事件的文件（`lib/handle-messages.ts` 和 `lib/handle-app-mention.ts`）
- 用于 Slack 事件的 API 端点（`POST`）（`api/events.ts`）

## 事件处理器

首先，让我们看一下我们的 API 路由（`api/events.ts`）：

```typescript
import type { SlackEvent } from '@slack/web-api';
import {
  assistantThreadMessage,
  handleNewAssistantMessage,
} from '../lib/handle-messages';
import { waitUntil } from '@vercel/functions';
import { handleNewAppMention } from '../lib/handle-app-mention';
import { verifyRequest, getBotId } from '../lib/slack-utils';

export async function POST(request: Request) {
  const rawBody = await request.text();
  const payload = JSON.parse(rawBody);
  const requestType = payload.type as 'url_verification' | 'event_callback';

  // 参见 https://api.slack.com/events/url_verification
  if (requestType === 'url_verification') {
    return new Response(payload.challenge, { status: 200 });
  }

  await verifyRequest({ requestType, request, rawBody });

  try {
    const botUserId = await getBotId();

    const event = payload.event as SlackEvent;

    if (event.type === 'app_mention') {
      waitUntil(handleNewAppMention(event, botUserId));
    }

    if (event.type === 'assistant_thread_started') {
      waitUntil(assistantThreadMessage(event));
    }

    if (
      event.type === 'message' &&
      !event.subtype &&
      event.channel_type === 'im' &&
      !event.bot_id &&
      !event.bot_profile &&
      event.bot_id !== botUserId
    ) {
      waitUntil(handleNewAssistantMessage(event, botUserId));
    }

    return new Response('Success!', { status: 200 });
  } catch (error) {
    console.error('Error generating response', error);
    return new Response('Error generating response', { status: 500 });
  }
}
```

该文件定义了一个 `POST` 函数，用于处理来自 Slack 的传入请求。首先，您检查请求类型，看它是否是 URL 验证请求。如果是，您使用 Slack 提供的挑战字符串进行响应。如果是事件回调，您验证请求，然后即可访问事件数据。这就是您可以实现事件处理逻辑的地方。

然后，您处理三种类型的事件：`app_mention`、`assistant_thread_started` 和 `message`：

- 对于 `app_mention`，您使用事件和机器人用户 ID 调用 `handleNewAppMention`。
- 对于 `assistant_thread_started`，您使用事件调用 `assistantThreadMessage`。
- 对于 `message`，您使用事件和机器人用户 ID 调用 `handleNewAssistantMessage`。

最后，您向 Slack 响应成功消息。请注意，每个处理函数都包装在 `waitUntil` 函数中。让我们看看这意味着什么以及为什么它很重要。

### waitUntil 函数

Slack 期望在 3 秒内收到响应，以确认请求正在被处理。然而，生成 AI 响应可能需要更长时间。如果您未在 3 秒内响应 Slack 请求，Slack 将发送另一个请求，导致您的 API 路由再次被调用，再次调用 LLM，并最终再次向用户发送响应。为了解决这个问题，您可以使用 `waitUntil` 函数，它允许您在响应发送后运行 AI 逻辑，而不会阻塞响应本身。

这意味着，您的 API 端点将：

1. 立即响应 Slack（3 秒内）
2. 异步继续处理消息
3. 在 AI 响应准备好时发送它

## 事件处理器

让我们看看每种事件类型当前是如何处理的。

### 应用提及

当用户在频道中提及您的机器人时，会触发 `app_mention` 事件。`handle-app-mention.ts` 中的 `handleNewAppMention` 函数处理这些提及：

1. 检查消息是否来自机器人，以避免无限响应循环
2. 创建一个状态更新器，显示机器人正在“思考”
3. 如果提及在线程中，则检索线程历史
4. 使用消息内容调用 LLM（使用您将在下一节中实现的 `generateResponse` 函数）
5. 使用 AI 响应更新初始的“思考”消息

以下是 `handleNewAppMention` 函数的代码：

```typescript filename="lib/handle-app-mention.ts"
import { AppMentionEvent } from '@slack/web-api';
import { client, getThread } from './slack-utils';
import { generateResponse } from './ai';

const updateStatusUtil = async (
  initialStatus: string,
  event: AppMentionEvent,
) => {
  const initialMessage = await client.chat.postMessage({
    channel: event.channel,
    thread_ts: event.thread_ts ?? event.ts,
    text: initialStatus,
  });

  if (!initialMessage || !initialMessage.ts)
    throw new Error('Failed to post initial message');

  const updateMessage = async (status: string) => {
    await client.chat.update({
      channel: event.channel,
      ts: initialMessage.ts as string,
      text: status,
    });
  };
  return updateMessage;
};

export async function handleNewAppMention(
  event: AppMentionEvent,
  botUserId: string,
) {
  console.log('Handling app mention');
  if (event.bot_id || event.bot_id === botUserId || event.bot_profile) {
    console.log('Skipping app mention');
    return;
  }

  const { thread_ts, channel } = event;
  const updateMessage = await updateStatusUtil('is thinking...', event);

  if (thread_ts) {
    const messages = await getThread(channel, thread_ts, botUserId);
    const result = await generateResponse(messages, updateMessage);
    updateMessage(result);
  } else {
    const result = await generateResponse(
      [{ role: 'user', content: event.text }],
      updateMessage,
    );
    updateMessage(result);
  }
}
```

现在让我们看看新的助手线程和消息是如何处理的。

### 助手线程消息

当用户与您的助手开始一个线程时，会触发 `assistant_thread_started` 事件。`handle-messages.ts` 中的 `assistantThreadMessage` 函数处理此事件：

1. 向线程发布欢迎消息
2. 设置建议提示词，帮助用户入门

以下是 `assistantThreadMessage` 函数的代码：

```typescript filename="lib/handle-messages.ts"
import type { AssistantThreadStartedEvent } from '@slack/web-api';
import { client } from './slack-utils';

export async function assistantThreadMessage(
  event: AssistantThreadStartedEvent,
) {
  const { channel_id, thread_ts } = event.assistant_thread;
  console.log(`Thread started: ${channel_id} ${thread_ts}`);
  console.log(JSON.stringify(event));

  await client.chat.postMessage({
    channel: channel_id,
    thread_ts: thread_ts,
    text: "Hello, I'm an AI assistant built with the AI SDK by Vercel!",
  });

  await client.assistant.threads.setSuggestedPrompts({
    channel_id: channel_id,
    thread_ts: thread_ts,
    prompts: [
      {
        title: 'Get the weather',
        message: 'What is the current weather in London?',
      },
      {
        title: 'Get the news',
        message: 'What is the latest Premier League news from the BBC?',
      },
    ],
  });
}
```

### 私信

对于发送给您的机器人的私信，会触发 `message` 事件，并由 `handle-messages.ts` 中的 `handleNewAssistantMessage` 函数处理：

1. 验证消息不是来自机器人
2. 更新状态以显示正在生成响应
3. 检索对话历史
4. 使用对话上下文调用 LLM
5. 将 LLM 的响应发布到线程

以下是 `handleNewAssistantMessage` 函数的代码：

```typescript filename="lib/handle-messages.ts"
import type { GenericMessageEvent } from '@slack/web-api';
import { client, getThread } from './slack-utils';
import { generateResponse } from './ai';

export async function handleNewAssistantMessage(
  event: GenericMessageEvent,
  botUserId: string,
) {
  if (
    event.bot_id ||
    event.bot_id === botUserId ||
    event.bot_profile ||
    !event.thread_ts
  )
    return;

  const { thread_ts, channel } = event;
  const updateStatus = updateStatusUtil(channel, thread_ts);
  updateStatus('is thinking...');

  const messages = await getThread(channel, thread_ts, botUserId);
  const result = await generateResponse(messages, updateStatus);

  await client.chat.postMessage({
    channel: channel,
    thread_ts: thread_ts,
    text: result,
    unfurl_links: false,
    blocks: [
      {
        type: 'section',
        text: {
          type: 'mrkdwn',
          text: result,
        },
      },
    ],
  });

  updateStatus('');
}
```

事件处理器就绪后，让我们现在实现 AI 逻辑。

## 实现 AI 逻辑

我们应用程序的核心是 `lib/generate-response.ts` 中的 `generateResponse` 函数，它使用 AI SDK 处理消息并生成响应。

以下是实现方法：

```typescript filename="lib/generate-response.ts"
import { generateText, ModelMessage } from 'ai';
__PROVIDER_IMPORT__;

export const generateResponse = async (
  messages: ModelMessage[],
  updateStatus?: (status: string) => void,
) => {
  const { text } = await generateText({
    model: __MODEL__,
    system: `You are a Slack bot assistant. Keep your responses concise and to the point.
    - Do not tag users.
    - Current date is: ${new Date().toISOString().split('T')[0]}`,
    messages,
  });

  // Convert markdown to Slack mrkdwn format
  return text.replace(/\[(.*?)\]\((.*?)\)/g, '<$2|$1>').replace(/\*\*/g, '*');
};
```

这个基本实现：

1. 使用 AI SDK 的 `generateText` 函数调用 Anthropic 的 `claude-sonnet-4.5` 模型
2. 提供系统提示词来引导模型的行为
3. 将响应格式化为 Slack 的 markdown 格式

## 使用工具增强

AI SDK 的真正强大之处在于工具，它们使您的机器人能够执行操作。让我们添加两个有用的工具：

```typescript filename="lib/generate-response.ts"
import { generateText, tool, ModelMessage, isStepCount } from 'ai';
__PROVIDER_IMPORT__;
import { z } from 'zod';
import { exa } from './utils';

export const generateResponse = async (
  messages: ModelMessage[],
  updateStatus?: (status: string) => void,
) => {
  const { text } = await generateText({
    model: __MODEL__,
    system: `You are a Slack bot assistant. Keep your responses concise and to the point.
    - Do not tag users.
    - Current date is: ${new Date().toISOString().split('T')[0]}
    - Always include sources in your final response if you use web search.`,
    messages,
    stopWhen: isStepCount(10),
    tools: {
      getWeather: tool({
        description: 'Get the current weather at a location',
        inputSchema: z.object({
          latitude: z.number(),
          longitude: z.number(),
          city: z.string(),
        }),
        execute: async ({ latitude, longitude, city }) => {
          updateStatus?.(`is getting weather for ${city}...`);

          const response = await fetch(
            `https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,weathercode,relativehumidity_2m&timezone=auto`,
          );

          const weatherData = await response.json();
          return {
            temperature: weatherData.current.temperature_2m,
            weatherCode: weatherData.current.weathercode,
            humidity: weatherData.current.relativehumidity_2m,
            city,
          };
        },
      }),
      searchWeb: tool({
        description: 'Use this to search the web for information',
        inputSchema: z.object({
          query: z.string(),
          specificDomain: z
            .string()
            .nullable()
            .describe(
              'a domain to search if the user specifies e.g. bbc.com. Should be only the domain name without the protocol',
            ),
        }),
        execute: async ({ query, specificDomain }) => {
          updateStatus?.(`is searching the web for ${query}...`);
          const { results } = await exa.searchAndContents(query, {
            livecrawl: 'always',
            numResults: 3,
            includeDomains: specificDomain ? [specificDomain] : undefined,
          });

          return {
            results: results.map(result => ({
              title: result.title,
              url: result.url,
              snippet: result.text.slice(0, 1000),
            })),
          };
        },
      }),
    },
  });

  // Convert markdown to Slack mrkdwn format
  return text.replace(/\[(.*?)\]\((.*?)\)/g, '<$2|$1>').replace(/\*\*/g, '*');
};
```

在这个更新的实现中：

1. 您添加了两个工具：
   - `getWeather`：获取指定位置的天气数据
   - `searchWeb`：使用 Exa API 搜索网络信息

2. 您设置了 `stopWhen: isStepCount(10)` 以启用多步对话。这定义了您的智能体的停止条件，即当模型生成工具调用时。这将自动将任何工具结果发送回 LLM，以触发 LLM 认为必要的额外工具调用或响应。这将您的 LLM 调用从一次性操作转变为多步智能体流程。

## 工作原理

当用户与您的机器人交互时：

1. Slack 事件被接收并由您的 API 端点处理
2. 用户的消息和线程历史被传递给 `generateResponse` 函数
3. AI SDK 处理消息，并可能根据需要调用工具
4. 响应被格式化为 Slack 格式并发送回用户

工具会根据用户的意图自动调用。例如，如果用户问“伦敦的天气怎么样？”，AI 将：

1. 识别这是一个天气查询
2. 使用伦敦的坐标（由 LLM 推断）调用 `getWeather` 工具
3. 处理天气数据
4. 生成最终响应，回答用户的问题

## 部署应用

1. 安装 Vercel CLI

<Snippet text={['pnpm install -g vercel']} />

2. 部署应用

<Snippet text={['vercel deploy']} />

3. 复制部署 URL，并更新 Slack 应用的事件订阅，指向您的 Vercel URL
4. 前往您项目的部署设置（您的项目 -> 设置 -> 环境变量）并添加您的环境变量

```bash
SLACK_BOT_TOKEN=your_slack_bot_token
SLACK_SIGNING_SECRET=your_slack_signing_secret
OPENAI_API_KEY=your_openai_api_key
EXA_API_KEY=your_exa_api_key
```

<Note>
  更新环境变量后，请确保重新部署您的应用。
</Note>

5. 返回 [https://api.slack.com/](https://api.slack.com/) 并导航到“事件订阅”页面。启用事件并添加您的部署 URL。

```bash
https://your-vercel-url.vercel.app/api/events
```

6. 在事件订阅页面上，订阅以下事件。
   - `app_mention`
   - `assistant_thread_started`
   - `message:im`

最后，前往 Slack 并通过向机器人发送消息来测试应用。

## 后续步骤

您已经构建了一个由 AI SDK 驱动的 Slack 聊天机器人！以下是一些扩展它的方法：

1. 为特定用户添加记忆，为 LLM 提供先前交互的上下文
2. 实现更多工具，如数据库查询或知识库搜索
3. 添加对使用块进行丰富消息格式化的支持
4. 添加分析以跟踪使用模式

<Note>
  在生产环境中，建议实现一个健壮的队列系统，以确保消息得到正确处理。
</Note>

有关具有持久线程会话、输入指示器、审批按钮和多频道支持的框架管理替代方案，请参阅 [eve 的 Slack 频道](https://eve.dev/docs/channels/slack)。
