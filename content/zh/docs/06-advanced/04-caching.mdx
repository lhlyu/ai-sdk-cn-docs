---
title: 缓存
description: 使用 AI SDK 时如何处理缓存
---

# 缓存响应

根据你构建的应用程序类型，你可能希望缓存从 AI 提供商收到的响应，至少是临时缓存。

## 使用语言模型中间件（推荐）

缓存响应的推荐方法是使用[语言模型中间件](/docs/ai-sdk-core/middleware)和 [`simulateReadableStream`](/docs/reference/ai-sdk-core/simulate-readable-stream) 函数。

语言模型中间件是一种通过拦截和修改对语言模型的调用来增强语言模型行为的方式。让我们看看如何使用语言模型中间件来缓存响应。

```ts filename="ai/middleware.ts"
import { Redis } from '@upstash/redis';
import {
  type LanguageModelV4,
  type LanguageModelV4Middleware,
  type LanguageModelV4StreamPart,
  simulateReadableStream,
} from 'ai';

const redis = new Redis({
  url: process.env.KV_URL,
  token: process.env.KV_TOKEN,
});

export const cacheMiddleware: LanguageModelV4Middleware = {
  wrapGenerate: async ({ doGenerate, params }) => {
    const cacheKey = JSON.stringify(params);

    const cached = (await redis.get(cacheKey)) as Awaited<
      ReturnType<LanguageModelV4['doGenerate']>
    > | null;

    if (cached !== null) {
      return {
        ...cached,
        response: {
          ...cached.response,
          timestamp: cached?.response?.timestamp
            ? new Date(cached?.response?.timestamp)
            : undefined,
        },
      };
    }

    const result = await doGenerate();

    redis.set(cacheKey, result);

    return result;
  },
  wrapStream: async ({ doStream, params }) => {
    const cacheKey = JSON.stringify(params);

    // 检查结果是否在缓存中
    const cached = await redis.get(cacheKey);

    // 如果已缓存，返回一个模拟的 ReadableStream，输出缓存的结果
    if (cached !== null) {
      // 格式化缓存响应中的时间戳
      const formattedChunks = (cached as LanguageModelV4StreamPart[]).map(p => {
        if (p.type === 'response-metadata' && p.timestamp) {
          return { ...p, timestamp: new Date(p.timestamp) };
        } else return p;
      });
      return {
        stream: simulateReadableStream({
          initialDelayInMs: 0,
          chunkDelayInMs: 10,
          chunks: formattedChunks,
        }),
      };
    }

    // 如果未缓存，继续流式传输
    const { stream, ...rest } = await doStream();

    const fullResponse: LanguageModelV4StreamPart[] = [];

    const transformStream = new TransformStream<
      LanguageModelV4StreamPart,
      LanguageModelV4StreamPart
    >({
      transform(chunk, controller) {
        fullResponse.push(chunk);
        controller.enqueue(chunk);
      },
      flush() {
        // 流式传输完成后，将完整响应存入缓存
        redis.set(cacheKey, fullResponse);
      },
    });

    return {
      stream: stream.pipeThrough(transformStream),
      ...rest,
    };
  },
};
```

<Note>
  此示例使用 `@upstash/redis` 来存储和检索助手的响应，但你可以使用任何你喜欢的 KV 存储提供商。
</Note>

`LanguageModelV4Middleware` 有两个方法：`wrapGenerate` 和 `wrapStream`。使用 [`generateText`](/docs/reference/ai-sdk-core/generate-text) 时会调用 `wrapGenerate`，而使用 [`streamText`](/docs/reference/ai-sdk-core/stream-text) 时会调用 `wrapStream`。

对于 `wrapGenerate`，你可以直接缓存响应。而对于 `wrapStream`，你可以缓存流式数据块的数组，然后使用 [`simulateReadableStream`](/docs/ai-sdk-core/testing#simulate-data-stream-protocol-responses) 函数创建一个模拟的 `ReadableStream` 来返回缓存响应。这样，缓存响应会像模型生成时一样逐块返回。你可以通过调整 `simulateReadableStream` 的 `initialDelayInMs` 和 `chunkDelayInMs` 参数来控制初始延迟和块之间的延迟。

你可以在我们的[缓存中间件配方](/cookbook/next/caching-middleware)中查看在 Next.js 应用程序中使用 Redis 进行缓存的完整示例。

## 使用生命周期回调

另外，每个 AI SDK 核心函数都有特殊的生命周期回调可供使用。其中可能感兴趣的是 `onEnd`，它在生成完成时被调用。这是你可以缓存完整响应的地方。

以下是一个示例，展示如何使用 Vercel KV 和 Next.js 将 OpenAI 响应缓存 1 小时：

此示例使用 [Upstash Redis](https://upstash.com/docs/redis/overall/getstarted) 和 Next.js 将响应缓存 1 小时。

```tsx filename="app/api/chat/route.ts"
import {
  convertToModelMessages,
  createUIMessageStreamResponse,
  streamText,
  toUIMessageStream,
  UIMessage,
} from 'ai';
__PROVIDER_IMPORT__;
import { Redis } from '@upstash/redis';

// 允许流式响应最长 30 秒
export const maxDuration = 30;

const redis = new Redis({
  url: process.env.KV_URL,
  token: process.env.KV_TOKEN,
});

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  // 根据请求生成一个键：
  const key = JSON.stringify(messages);

  // 检查是否有缓存响应
  const cached = (await redis.get(key)) as string | null;
  if (cached != null) {
    return new Response(cached, {
      status: 200,
      headers: { 'Content-Type': 'text/plain' },
    });
  }

  // 调用语言模型：
  const result = streamText({
    model: __MODEL__,
    messages: await convertToModelMessages(messages),
    async onEnd({ text }) {
      // 缓存响应文本：
      await redis.set(key, text);
      await redis.expire(key, 60 * 60);
    },
  });

  // 返回流式响应
  return createUIMessageStreamResponse({
    stream: toUIMessageStream({ stream: result.stream }),
  });
}
```
