---
title: "工作流工具"
description: "将 HarnessAgent 轮次作为持久化 Workflow DevKit 工作流运行。"
---

# 工作流工具

`@ai-sdk/workflow-harness` 提供了在 [workflow](https://vercel.com/docs/workflow) 中运行 `HarnessAgent` 轮次的辅助函数。

该包提供了一个可序列化的状态机，以及用于时间切片和语义智能体步骤轮次的运行器。您可以从自己的 `'use workflow'` 和 `'use step'` 函数中调用相应的运行器。

核心 harness 和工作流文件与框架无关。下面显示的 HTTP 处理程序以 Next.js 为例；请根据您的运行时和 Workflow SDK 集成进行调整。如果您使用 Next.js，请确保在遵循示例之前已[为 Workflow 配置了您的项目](https://workflow-sdk.dev/docs/getting-started/next)。

## 安装

<InstallPackages packages="@ai-sdk/workflow-harness workflow" />

除了工作流特定的包之外，还需要按照 [HarnessAgent](/docs/ai-sdk-harnesses/harness-agent) 中的说明安装核心 harness 包、harness 适配器和沙箱提供商。

## 配置 Harness Agent

智能体大部分情况下可以按照常规方式进行配置。使用语义智能体步骤时，将 `stopWhen` 设置为 `isStepCount(1)`，这样一次 `stream()` 调用即可完成一个智能体步骤。使用时间切片时则省略此项。

```ts filename='harness-workflow/agent.ts' highlight="13-17"
import { HarnessAgent } from '@ai-sdk/harness/agent';
import { claudeCode } from '@ai-sdk/harness-claude-code';
import { createVercelSandbox } from '@ai-sdk/sandbox-vercel';
import { isStepCount } from 'ai';

export const agent = new HarnessAgent({
  harness: claudeCode,
  sandbox: createVercelSandbox({
    runtime: 'node24',
    ports: [4000],
  }),
  instructions: 'You are a helpful coding assistant.',
  /*
   * 仅语义智能体步骤工作流需要。
   * 时间切片工作流请省略此项。
   */
  stopWhen: isStepCount(1),
});
```

## 使用语义智能体步骤

语义智能体步骤会在每个智能体步骤之后持久化 harness 轮次。使用上面显示的带高亮 `stopWhen` 选项的共享智能体进行配置，然后从 Workflow 步骤中调用 `runHarnessAgentStep()`。

### 定义智能体步骤

将 Workflow 步骤放在其自己的模块中，并在步骤体内动态导入智能体。这可以将智能体、沙箱提供商和其他 Node.js 依赖项排除在工作流捆绑包之外。

```ts filename='harness-workflow/agent-step.ts'
import {
  runHarnessAgentStep,
  type HarnessWorkflowState,
} from '@ai-sdk/workflow-harness';

export async function agentStep(
  state: HarnessWorkflowState,
): Promise<HarnessWorkflowState> {
  'use step';

  const { agent } = await import('./agent');

  return runHarnessAgentStep({
    agent,
    state,
  });
}
```

### 定义语义智能体步骤工作流

创建工作流状态，并在智能体有更多工作时持续调度 `agentStep()`。

```ts filename='harness-workflow/workflow.ts'
import { agentStep } from './agent-step';
import {
  createHarnessWorkflowState,
  finalizeHarnessWorkflow,
  type HarnessWorkflowInput,
} from '@ai-sdk/workflow-harness';

export async function agentWorkflow(input: {
  messages: NonNullable<HarnessWorkflowInput['messages']>;
  sessionId: string;
}) {
  'use workflow';

  let state = createHarnessWorkflowState(input);

  do {
    state = await agentStep(state);
  } while (state.status === 'ready_for_next_step');

  return finalizeHarnessWorkflow(state);
}
```

<Note>
  此示例仅涵盖工作流执行基础。对于多轮对话，您必须在工作流运行之间持久化 harness 会话的 `resumeFrom` 状态。请参阅 [恢复持久化](#resume-persistence)。
</Note>

每个 `ready_for_next_step` 结果都带有 `continueFrom`，它允许下一个 Workflow 步骤继续同一个未完成的轮次。当轮次完成时，`finalizeHarnessWorkflow()` 返回其结果，如果工作流失败则抛出异常。

### 启动语义智能体步骤工作流

从服务器端代码启动工作流。此 Next.js 路由转换 AI SDK UI 消息，启动 `agentWorkflow()`，并返回工作流的 AI SDK UI 消息流。传递转换后的消息可以让 `HarnessAgent` 区分新的用户轮次与工具审批和工具结果延续。

```ts filename='app/api/harness-workflow/route.ts'
import { agentWorkflow } from '../../../harness-workflow/workflow';
import {
  convertToModelMessages,
  createUIMessageStreamResponse,
  type UIMessage,
  type UIMessageChunk,
} from 'ai';
import { start } from 'workflow/api';

export async function POST(request: Request) {
  const body: {
    id?: string;
    messages: UIMessage[];
  } = await request.json();

  if (!body.id) {
    return new Response('Missing chat ID', { status: 400 });
  }

  const messages = await convertToModelMessages(body.messages);
  const run = await start(agentWorkflow, [
    {
      messages,
      sessionId: body.id,
    },
  ]);

  return createUIMessageStreamResponse({
    stream: run.readable as ReadableStream<UIMessageChunk>,
  });
}
```

`sessionId` 为沙箱提供了跨工作流运行的稳定身份。将 `agent.ts`、`agent-step.ts`、`workflow.ts` 和路由放在单独的模块中，这样工作流捆绑包就不会包含 Node 重量级的智能体、沙箱或框架依赖项。

## 使用时间切片

时间切片会在墙钟时间边界持久化长时间运行的 harness 轮次。省略共享智能体中高亮的 `stopWhen` 选项，然后从 Workflow 步骤中调用 `runHarnessAgentTimeSlice()`。默认使用 750 秒的预算；传入 `timeSliceSeconds` 以选择不同的预算。

### 定义时间切片步骤

与语义智能体步骤一样，将 Workflow 步骤放在其自己的模块中，并在步骤体内动态导入智能体。

```ts filename='harness-workflow/time-slice-step.ts'
import {
  runHarnessAgentTimeSlice,
  type HarnessWorkflowState,
} from '@ai-sdk/workflow-harness';

export async function timeSliceStep(
  state: HarnessWorkflowState,
): Promise<HarnessWorkflowState> {
  'use step';

  const { agent } = await import('./agent');

  return runHarnessAgentTimeSlice({
    agent,
    state,
  });
}
```

### 定义时间切片工作流

创建工作流状态，并在智能体有更多工作时持续调度 `timeSliceStep()`。

```ts filename='harness-workflow/workflow.ts'
import { timeSliceStep } from './time-slice-step';
import {
  createHarnessWorkflowState,
  finalizeHarnessWorkflow,
  type HarnessWorkflowInput,
} from '@ai-sdk/workflow-harness';

export async function timeSliceWorkflow(input: {
  messages: NonNullable<HarnessWorkflowInput['messages']>;
  sessionId: string;
}) {
  'use workflow';

  let state = createHarnessWorkflowState(input);

  do {
    state = await timeSliceStep(state);
  } while (state.status === 'ready_for_next_step');

  return finalizeHarnessWorkflow(state);
}
```

<Note>
  此示例仅涵盖工作流执行基础。对于多轮对话，您必须在工作流运行之间持久化 harness 会话的 `resumeFrom` 状态。请参阅 [恢复持久化](#resume-persistence)。
</Note>

每个 `ready_for_next_step` 结果都带有 `continueFrom`，它允许下一个 Workflow 步骤继续同一个未完成的轮次。当轮次完成时，`finalizeHarnessWorkflow()` 返回其结果，如果工作流失败则抛出异常。

### 启动时间切片工作流

从服务器端代码启动工作流。与语义智能体步骤一样，传递转换后的消息，以便 `HarnessAgent` 可以区分新的用户轮次与工具审批和工具结果延续。

```ts filename='app/api/harness-workflow/route.ts'
import { timeSliceWorkflow } from '../../../harness-workflow/workflow';
import {
  convertToModelMessages,
  createUIMessageStreamResponse,
  type UIMessage,
  type UIMessageChunk,
} from 'ai';
import { start } from 'workflow/api';

export async function POST(request: Request) {
  const body: {
    id?: string;
    messages: UIMessage[];
  } = await request.json();

  if (!body.id) {
    return new Response('Missing chat ID', { status: 400 });
  }

  const messages = await convertToModelMessages(body.messages);
  const run = await start(timeSliceWorkflow, [
    {
      messages,
      sessionId: body.id,
    },
  ]);

  return createUIMessageStreamResponse({
    stream: run.readable as ReadableStream<UIMessageChunk>,
  });
}
```

`sessionId` 为沙箱提供了跨工作流运行的稳定身份。将 `agent.ts`、`time-slice-step.ts`、`workflow.ts` 和路由放在单独的模块中，这样工作流捆绑包就不会包含 Node 重量级的智能体、沙箱或框架依赖项。

## 恢复持久化

Workflow 会自动持久化每个步骤返回的 `HarnessWorkflowState`（包括 `continueFrom`），持续时间为当前工作流运行期间。要在单独的用户轮次工作流运行之间继续原生 harness 会话，请按 `sessionId` 持久化不透明的 `resumeFrom` 状态。

存储实现对于语义智能体步骤和时间切片是相同的。此示例使用 Workflow 步骤，因为文件系统访问必须保持在工作流函数之外。在生产环境中，请使用持久化存储而不是本地文件。

```ts filename='harness-workflow/resume-store.ts'
import type { HarnessV1ResumeSessionState } from '@ai-sdk/harness';
import { safeParseJSON } from '@ai-sdk/provider-utils';

const RESUME_DIR = '.harness-sessions';

function fileName(sessionId: string): string {
  return `${sessionId.replace(/[^a-zA-Z0-9_-]/g, '_')}.json`;
}

export async function loadResumeStep(
  sessionId: string,
): Promise<HarnessV1ResumeSessionState | undefined> {
  'use step';

  const { readFile } = await import('node:fs/promises');
  const { join } = await import('node:path');

  let text: string;
  try {
    text = await readFile(
      join(process.cwd(), RESUME_DIR, fileName(sessionId)),
      'utf8',
    );
  } catch {
    return undefined;
  }

  const parsed = await safeParseJSON({ text });

  return parsed.success
    ? (parsed.value as unknown as HarnessV1ResumeSessionState)
    : undefined;
}

export async function persistResumeStep({
  sessionId,
  resumeState,
}: {
  sessionId: string;
  resumeState: HarnessV1ResumeSessionState | undefined;
}): Promise<void> {
  'use step';

  if (!resumeState) return;

  const { mkdir, writeFile } = await import('node:fs/promises');
  const { join } = await import('node:path');
  const dir = join(process.cwd(), RESUME_DIR);

  await mkdir(dir, { recursive: true });
  await writeFile(join(dir, fileName(sessionId)), JSON.stringify(resumeState));
}
```

在创建工作流状态之前加载之前的 `resumeFrom` 状态，然后在执行循环之后持久化更新后的值。两种工作流方法的集成点相同。

### 语义智能体步骤工作流

```ts filename='harness-workflow/workflow.ts'
import { agentStep } from './agent-step';
import { loadResumeStep, persistResumeStep } from './resume-store';
import {
  createHarnessWorkflowState,
  finalizeHarnessWorkflow,
  type HarnessWorkflowInput,
} from '@ai-sdk/workflow-harness';

export async function agentWorkflow(input: {
  messages: NonNullable<HarnessWorkflowInput['messages']>;
  sessionId: string;
}) {
  'use workflow';

  const resumeFrom = await loadResumeStep(input.sessionId);
  let state = createHarnessWorkflowState({ ...input, resumeFrom });

  do {
    state = await agentStep(state);
  } while (state.status === 'ready_for_next_step');

  await persistResumeStep({
    sessionId: state.sessionId,
    resumeState: state.resumeFrom,
  });

  return finalizeHarnessWorkflow(state);
}
```

### 时间切片工作流

```ts filename='harness-workflow/workflow.ts'
import { loadResumeStep, persistResumeStep } from './resume-store';
import { timeSliceStep } from './time-slice-step';
import {
  createHarnessWorkflowState,
  finalizeHarnessWorkflow,
  type HarnessWorkflowInput,
} from '@ai-sdk/workflow-harness';

export async function timeSliceWorkflow(input: {
  messages: NonNullable<HarnessWorkflowInput['messages']>;
  sessionId: string;
}) {
  'use workflow';

  const resumeFrom = await loadResumeStep(input.sessionId);
  let state = createHarnessWorkflowState({ ...input, resumeFrom });

  do {
    state = await timeSliceStep(state);
  } while (state.status === 'ready_for_next_step');

  await persistResumeStep({
    sessionId: state.sessionId,
    resumeState: state.resumeFrom,
  });

  return finalizeHarnessWorkflow(state);
}
```

## 相关

- [HarnessAgent](/docs/ai-sdk-harnesses/harness-agent)
- [Harness 适配器](/docs/ai-sdk-harnesses/harness-adapters)
- [UI](/docs/ai-sdk-harnesses/ui)
