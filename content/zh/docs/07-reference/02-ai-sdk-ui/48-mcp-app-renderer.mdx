---
title: experimental_MCPAppRenderer
description: 使用 @ai-sdk/react 渲染 MCP 应用的 API 参考文档。
---

# `experimental_MCPAppRenderer`

<Note type="warning">
  `experimental_MCPAppRenderer` 是实验性功能，在未来的版本中可能会发生变化。
</Note>

`experimental_MCPAppRenderer` 用于渲染 AI SDK 工具 UI 部分的 MCP 应用。它会检测工具部分上的 MCP 应用元数据，加载应用资源，在沙箱代理 iframe 中渲染应用，并在 iframe 与宿主应用程序之间桥接 MCP 应用的 JSON-RPC 消息。

对于没有 MCP 应用元数据的工具部分，该组件会渲染 `fallback`。

## 导入

<Snippet
  text={`import { experimental_MCPAppRenderer as MCPAppRenderer } from "@ai-sdk/react"`}
  prompt={false}
/>

## 示例

```tsx
'use client';

import {
  experimental_MCPAppRenderer as MCPAppRenderer,
  type MCPAppBridgeHandlers,
  type MCPAppMetadata,
  type MCPAppResource,
  type MCPAppSandboxConfig,
} from '@ai-sdk/react';
import { isToolUIPart } from 'ai';

const sandbox = {
  url: '/mcp-app-sandbox',
  className: 'h-80 w-full rounded-lg border',
  style: { border: 0 },
} satisfies MCPAppSandboxConfig;

async function loadResource(app: MCPAppMetadata): Promise<MCPAppResource> {
  const response = await fetch('/api/mcp-app-host/read-resource', {
    method: 'POST',
    body: JSON.stringify({ uri: app.resourceUri }),
  });

  if (!response.ok) {
    throw new Error('加载 MCP 应用资源失败');
  }

  return response.json();
}

const handlers: MCPAppBridgeHandlers = {
  callTool: params =>
    fetch('/api/mcp-app-host/call-tool', {
      method: 'POST',
      body: JSON.stringify(params),
    }).then(response => response.json()),
  openLink: ({ url }) => {
    window.open(url, '_blank', 'noopener,noreferrer');
    return {};
  },
};

export function MessagePart({ part }: { part: unknown }) {
  if (!isToolUIPart(part)) {
    return null;
  }

  return (
    <MCPAppRenderer
      part={part}
      loadResource={loadResource}
      handlers={handlers}
      sandbox={sandbox}
      fallback={null}
    />
  );
}
```

## Props

<PropertiesTable
  content={[
    {
      name: 'part',
      type: 'ToolUIPart<UITools> | DynamicToolUIPart',
      description:
        'AI SDK 工具 UI 部分。渲染器会在 `part.toolMetadata.mcp.app` 中查找 MCP 应用元数据。',
    },
    {
      name: 'sandbox',
      type: 'MCPAppSandboxConfig',
      description:
        '用于托管应用的外部沙箱代理 iframe 的配置。',
    },
    {
      name: 'resource',
      type: 'MCPAppResource',
      isOptional: true,
      description:
        '预加载的 MCP 应用资源。请提供 `resource` 或 `loadResource` 之一。',
    },
    {
      name: 'loadResource',
      type: '(app: MCPAppMetadata) => Promise<MCPAppResource>',
      isOptional: true,
      description:
        '根据工具部分上的应用元数据加载 MCP 应用资源。',
    },
    {
      name: 'handlers',
      type: 'MCPAppBridgeHandlers',
      isOptional: true,
      description:
        '用于处理 iframe 请求的回调函数，例如 `tools/call`、`resources/read`、`ui/open-link` 以及显示模式变更。',
    },
    {
      name: 'hostInfo',
      type: '{ name: string; version: string }',
      isOptional: true,
      description:
        '在 `ui/initialize` 握手期间返回给应用的宿主身份信息。',
    },
    {
      name: 'hostContext',
      type: 'MCPAppHostContext',
      isOptional: true,
      description:
        '发送给应用的宿主上下文，例如主题、显示模式以及可用的显示模式。',
    },
    {
      name: 'fallback',
      type: 'ReactNode',
      isOptional: true,
      description:
        '在资源加载中、加载失败或工具部分不是 MCP 应用时渲染的内容。',
    },
  ]}
/>

## 沙箱配置

<PropertiesTable
  content={[
    {
      name: 'url',
      type: 'string | URL',
      description:
        '沙箱代理 iframe 的 URL。代理从宿主接收应用 HTML 并创建内部应用 iframe。',
    },
    {
      name: 'title',
      type: 'string',
      isOptional: true,
      description: '可访问的 iframe 标题。',
    },
    {
      name: 'className',
      type: 'string',
      isOptional: true,
      description: '应用于外部 iframe 的类名。',
    },
    {
      name: 'style',
      type: 'CSSProperties',
      isOptional: true,
      description: '应用于外部 iframe 的内联样式。',
    },
    {
      name: 'targetOrigin',
      type: 'string',
      isOptional: true,
      description:
        '用于 `postMessage` 的目标源。默认为 `*`；在生产环境中请将其设置为您的沙箱源。',
    },
    {
      name: 'outerSandbox',
      type: 'string',
      isOptional: true,
      description:
        '外部代理 iframe 的沙箱属性。默认为 `allow-scripts allow-same-origin allow-forms`。',
    },
    {
      name: 'innerSandbox',
      type: 'string',
      isOptional: true,
      description:
        '发送给代理用于内部应用 iframe 的沙箱属性。默认为 `allow-scripts allow-forms`。',
    },
  ]}
/>

## 桥接处理器

`experimental_MCPAppRenderer` 使用这些处理器来响应 iframe 请求。在生产环境中，基于服务器的处理器应在调用 MCP 服务器之前验证授权和 MCP 应用工具可见性。

<PropertiesTable
  content={[
    {
      name: 'allowedTools',
      type: 'string[]',
      isOptional: true,
      description:
        '可选的客户端允许列表，在转发 `tools/call` 请求之前进行检查。',
    },
    {
      name: 'callTool',
      type: '(params: MCPAppToolCallParams) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理应用发起的 `tools/call` 请求。',
    },
    {
      name: 'readResource',
      type: '(params: { uri: string }) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理应用发起的 `resources/read` 请求。',
    },
    {
      name: 'listResources',
      type: '(params?: unknown) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理应用发起的 `resources/list` 请求。',
    },
    {
      name: 'openLink',
      type: '(params: { url: string }) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理应用发起的 `ui/open-link` 请求。',
    },
    {
      name: 'sendMessage',
      type: '(params: unknown) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理应用发起的 `ui/message` 请求。',
    },
    {
      name: 'updateModelContext',
      type: '(params: unknown) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理应用发起的 `ui/update-model-context` 请求。',
    },
    {
      name: 'requestDisplayMode',
      type: "(params: { mode: 'inline' | 'fullscreen' | 'pip' }) => Promise<{ mode: MCPAppDisplayMode }> | { mode: MCPAppDisplayMode }",
      isOptional: true,
      description: '处理应用发起的 `ui/request-display-mode` 请求。',
    },
    {
      name: 'onSizeChange',
      type: '(params: { width?: number; height?: number }) => void',
      isOptional: true,
      description: '当应用发送尺寸变更通知时调用。',
    },
    {
      name: 'onInitialized',
      type: '() => void',
      isOptional: true,
      description: '在应用发送 `ui/notifications/initialized` 后调用。',
    },
    {
      name: 'onRequestTeardown',
      type: '(params: unknown) => void',
      isOptional: true,
      description: '当应用请求拆除时调用。',
    },
    {
      name: 'onLog',
      type: '(params: unknown) => void',
      isOptional: true,
      description: '当应用发送日志通知时调用。',
    },
    {
      name: 'onError',
      type: '(error: Error) => void',
      isOptional: true,
      description:
        '当支持的 iframe 请求在处理过程中失败时调用。',
    },
  ]}
/>

## 参见

<ExampleLinks
  examples={[
    {
      title: 'MCP 应用指南',
      link: '/docs/ai-sdk-core/mcp-apps',
    },
    {
      title: 'MCP 应用辅助工具',
      link: '/docs/reference/ai-sdk-core/mcp-apps',
    },
  ]}
/>
