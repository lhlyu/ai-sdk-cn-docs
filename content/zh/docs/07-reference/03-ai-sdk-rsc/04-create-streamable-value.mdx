---
title: createStreamableValue
description: AI SDK RSC 中 createStreamableValue 函数的参考文档
---

# `createStreamableValue`

<Note type="warning">
  AI SDK RSC 目前处于实验阶段。生产环境建议使用 [AI SDK
  UI](/docs/ai-sdk-ui/overview)。关于从 RSC 迁移到 UI 的指导，请参阅我们的[迁移指南](/docs/ai-sdk-rsc/migrating-to-ui)。
</Note>

创建一个从服务器向客户端发送值的流。该值可以是任何可序列化的数据。

## 导入

<Snippet
  text={`import { createStreamableValue } from "@ai-sdk/rsc"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'value',
      type: 'any',
      description: 'RSC 支持的任何数据。例如，JSON。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'value',
      type: 'StreamableValue',
      description:
        '可流式值的值。可以从 Server Action 返回并由客户端接收。要读取流式值，请使用 `readStreamableValue` 或 `useStreamableValue` API。',
    },
  ]}
/>

### 方法

<PropertiesTable
  content={[
    {
      name: 'update',
      type: '(value: T) => StreamableValueWrapper',
      description: '用新值更新当前值。',
    },
    {
      name: 'append',
      type: '(value: T) => StreamableValueWrapper',
      description:
        '将增量字符串追加到当前值。要求可流式值的当前值必须是字符串。',
    },
    {
      name: 'done',
      type: '(value?: T) => StreamableValueWrapper',
      description:
        '标记值为最终状态。可以不带参数调用，也可以传入新值作为最终状态。一旦调用，值将无法再更新或追加。此方法始终需要调用，否则响应将卡在加载状态。',
    },
    {
      name: 'error',
      type: '(error: any) => StreamableValueWrapper',
      description:
        '表示值流中存在错误。当通过 `readStreamableValue` 或 `useStreamableValue` 在客户端消费时，该错误将被抛出。',
    },
  ]}
/>
