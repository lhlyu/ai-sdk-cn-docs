---
title: "AI_NoImageGeneratedError"
description: "了解如何修复 AI_NoImageGeneratedError"
---

# AI_NoImageGeneratedError

当 AI 提供商未能生成图像时，会出现此错误。
可能由以下原因导致：

- 模型未能生成响应。
- 模型生成了无效响应。

## 属性

- `message`：错误消息（可选，默认为 `'No image generated.'`）。
- `calls`：底层图像模型调用的结果，包括生成的图像、提供商元数据、响应元数据、警告和使用情况（可选）。
- `responses`：图像模型响应的元数据，包括时间戳、模型和标头（可选）。
- `cause`：错误的原因。您可以使用它进行更详细的错误处理（可选）。

## 检查此错误

您可以使用以下方法检查错误是否为 `AI_NoImageGeneratedError` 的实例：

```typescript
import { generateImage, NoImageGeneratedError } from 'ai';

try {
  await generateImage({ model, prompt });
} catch (error) {
  if (NoImageGeneratedError.isInstance(error)) {
    console.log('NoImageGeneratedError');
    console.log('Cause:', error.cause);
    console.log('Responses:', error.responses);

    for (const call of error.calls ?? []) {
      console.log('Provider metadata:', call.providerMetadata);
      console.log('Warnings:', call.warnings);
      console.log('Usage:', call.usage);
    }
  }
}
```
