---
title: AI_NoContentGeneratedError
description: 了解如何修复 AI_NoContentGeneratedError 错误
---

# AI_NoContentGeneratedError

当 AI 提供商未能生成内容时，会触发此错误。

## 属性

- `message`：错误消息（可选，默认为 `'No content generated.'`）

## 检查此错误

你可以使用以下方法检查某个错误是否为 `AI_NoContentGeneratedError` 的实例：

```typescript
import { NoContentGeneratedError } from 'ai';

if (NoContentGeneratedError.isInstance(error)) {
  // 处理错误
}
```
