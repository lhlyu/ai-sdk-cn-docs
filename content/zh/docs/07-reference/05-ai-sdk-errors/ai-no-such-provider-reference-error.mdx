---
title: AI_NoSuchProviderReferenceError
description: 了解如何修复 AI_NoSuchProviderReferenceError 错误
---

# AI_NoSuchProviderReferenceError

当提供商引用无法解析，因为在提供商引用映射中找不到指定的提供商时，会触发此错误。

## 属性

- `provider`：未找到的提供商
- `reference`：被搜索的完整提供商引用映射
- `message`：错误消息

## 检查此错误

你可以使用以下方法检查错误是否为 `AI_NoSuchProviderReferenceError` 的实例：

```typescript
import { NoSuchProviderReferenceError } from 'ai';

if (NoSuchProviderReferenceError.isInstance(error)) {
  // 处理错误
}
```
