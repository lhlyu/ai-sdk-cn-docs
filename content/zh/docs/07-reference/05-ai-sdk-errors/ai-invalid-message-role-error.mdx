---
title: AI_InvalidMessageRoleError
description: 了解如何修复 AI_InvalidMessageRoleError 错误
---

# AI_InvalidMessageRoleError

当提供了无效的消息角色时，会触发此错误。

## 属性

- `role`：无效的角色值
- `message`：错误消息（可选，根据 `role` 自动生成）

## 检查此错误

你可以使用以下方法检查错误是否为 `AI_InvalidMessageRoleError` 的实例：

```typescript
import { InvalidMessageRoleError } from 'ai';

if (InvalidMessageRoleError.isInstance(error)) {
  // 处理错误
}
```
