---
title: "experimental_generateVideo"
description: "experimental_generateVideo 的 API 参考。"
---

# `experimental_generateVideo()`

<Note>
  视频生成是一项实验性功能。该 API 在未来的版本中可能会发生变化。
</Note>

根据给定的提示词，使用视频模型生成视频。

它非常适合需要以编程方式生成视频的场景，例如创建视觉内容、动画或从图像生成视频。

```ts
import { experimental_generateVideo as generateVideo } from 'ai';
__PROVIDER_IMPORT__;

const { videos } = await generateVideo({
  model: __VIDEO_MODEL__,
  prompt: 'A cat walking on a treadmill',
  aspectRatio: '16:9',
});

console.log(videos);
```

## 导入

<Snippet
  text={`import { experimental_generateVideo } from "ai"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'model',
      type: 'VideoModelV4',
      description: '要使用的视频模型。',
    },
    {
      name: 'prompt',
      type: 'string | GenerateVideoPrompt',
      description: '用于生成视频的输入提示词。',
      properties: [
        {
          type: 'GenerateVideoPrompt',
          type: 'object',
          description:
            '用于视频生成的提示词对象，可包含可选的输入图像',
          parameters: [
            {
              name: 'image',
              type: 'DataContent',
              description:
                '用于图像转视频生成的输入图像。可以是 URL 字符串、base64 编码字符串、`Uint8Array`、`ArrayBuffer` 或 `Buffer`。',
            },
            {
              name: 'text',
              type: 'string',
              description: '文本提示词。',
            },
          ],
        },
      ],
    },
    {
      name: 'n',
      type: 'number',
      isOptional: true,
      description: '要生成的视频数量。默认值：1。',
    },
    {
      name: 'aspectRatio',
      type: 'string',
      isOptional: true,
      description:
        '要生成的视频的宽高比。格式：`{width}:{height}`，或使用 `'adaptive'` 来继承输入媒体（例如首帧图像或源视频）的宽高比。对 `'adaptive'` 的支持取决于具体提供商。',
    },
    {
      name: 'resolution',
      type: 'string',
      isOptional: true,
      description:
        '要生成的视频的分辨率。格式：`{width}x{height}`。',
    },
    {
      name: 'duration',
      type: 'number',
      isOptional: true,
      description: '视频的时长（秒）。',
    },
    {
      name: 'fps',
      type: 'number',
      isOptional: true,
      description: '视频的帧率。',
    },
    {
      name: 'seed',
      type: 'number',
      isOptional: true,
      description: '视频生成的随机种子。',
    },
    {
      name: 'frameImages',
      type: 'Array<{ image: DataContent; frameType: "first_frame" | "last_frame" }>',
      isOptional: true,
      description: '用于首尾帧生成的角色标记图像输入。',
    },
    {
      name: 'inputReferences',
      type: 'Array<DataContent | { data: DataContent; mediaType?: string }>',
      isOptional: true,
      description:
        '用于参考转视频生成的参考图像或视频输入。对于基于 URL 的视频引用，请使用带有显式 mediaType 的对象形式。提供商根据媒体类型路由每个引用，并在引用类型不受支持时发出警告。',
    },
    {
      name: 'generateAudio',
      type: 'boolean',
      isOptional: true,
      description:
        '模型是否应在生成视频的同时生成音频。',
    },
    {
      name: 'providerOptions',
      type: 'ProviderOptions',
      isOptional: true,
      description: '额外的提供商特定选项。',
    },
    {
      name: 'maxVideosPerCall',
      type: 'number',
      isOptional: true,
      description:
        '每次 API 调用生成的最大视频数量。当 n 超过此值时，将进行多次 API 调用。',
    },
    {
      name: 'maxRetries',
      type: 'number',
      isOptional: true,
      description: '最大重试次数。默认值：2。',
    },
    {
      name: 'abortSignal',
      type: 'AbortSignal',
      isOptional: true,
      description: '可选的取消信号，用于取消调用。',
    },
    {
      name: 'headers',
      type: 'Record<string, string>',
      isOptional: true,
      description: '请求的额外 HTTP 头。',
    },
    {
      name: 'download',
      type: '(options: { url: URL; abortSignal?: AbortSignal }) => Promise<{ data: Uint8Array; mediaType: string | undefined }>',
      isOptional: true,
      description:
        '用于从 URL 获取视频的自定义下载函数。使用 `ai` 中的 `createDownload()` 创建具有自定义大小限制的下载函数，例如 `createDownload({ maxBytes: 50 * 1024 * 1024 })`。默认值：内置下载，限制为 2 GiB。',
    },
    {
      name: 'poll',
      type: 'object',
      isOptional: true,
      description:
        '用于异步启动/状态流程的轮询配置。',
      properties: [
        {
          type: 'object',
          parameters: [
            {
              name: 'intervalMs',
              type: 'number',
              isOptional: true,
              description:
                '状态检查之间的间隔（毫秒）。默认值：5000。',
            },
            {
              name: 'timeoutMs',
              type: 'number',
              isOptional: true,
              description:
                '等待完成的最大时间（毫秒），包括等待 webhook 通知的时间。默认值：600000（10 分钟）。',
            },
            {
              name: 'delay',
              type: '(delayInMs: number, options?: { abortSignal?: AbortSignal }) => PromiseLike<void>',
              isOptional: true,
              description:
                '用于轮询间隔和 webhook 超时的自定义延迟实现。适用于持久化工作流中的 sleep 函数。默认值：基于内置定时器的延迟。',
            },
          ],
        },
      ],
    },
    {
      name: 'webhook',
      type: '() => PromiseLike<{ url: string; received: PromiseLike<VideoModelV4OperationWebhook> }>',
      isOptional: true,
      description:
        '用于支持 webhook 通知的提供商的 webhook 工厂。当提供此选项且模型支持 webhook 时，SDK 将使用 webhook 而不是轮询。该工厂应返回一个供提供商通知的 URL，以及一个在通知到达时解析的 `received` promise。也可以提供 `poll` 选项来配置 webhook 超时和轮询回退。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'video',
      type: 'GeneratedFile',
      description: '生成的第一个视频。',
      properties: [
        {
          type: 'GeneratedFile',
          parameters: [
            {
              name: 'base64',
              type: 'string',
              description: '视频的 base64 编码字符串。',
            },
            {
              name: 'uint8Array',
              type: 'Uint8Array',
              description: '视频的 Uint8Array 表示。',
            },
            {
              name: 'mediaType',
              type: 'string',
              description:
                '视频的 IANA 媒体类型（例如，video/mp4）。',
            },
          ],
        },
      ],
    },
    {
      name: 'videos',
      type: 'Array<GeneratedFile>',
      description: '生成的所有视频。',
      properties: [
        {
          type: 'GeneratedFile',
          parameters: [
            {
              name: 'base64',
              type: 'string',
              description: '视频的 base64 编码字符串。',
            },
            {
              name: 'uint8Array',
              type: 'Uint8Array',
              description: '视频的 Uint8Array 表示。',
            },
            {
              name: 'mediaType',
              type: 'string',
              description:
                '视频的 IANA 媒体类型（例如，video/mp4）。',
            },
          ],
        },
      ],
    },
    {
      name: 'warnings',
      type: 'Warning[]',
      description:
        '来自模型提供商的警告（例如，不支持的设置）。',
    },
    {
      name: 'providerMetadata',
      type: 'VideoModelProviderMetadata',
      isOptional: true,
      description:
        '来自提供商的可选元数据。外层键是提供商名称。内层值是元数据。元数据中通常存在一个 `videos` 键，它是一个与顶层 `videos` 键长度相同的数组。具体细节取决于提供商。',
    },
    {
      name: 'responses',
      type: 'Array<VideoModelResponseMetadata>',
      description:
        '来自提供商的响应元数据。如果我们对模型进行了多次调用，则可能存在多个响应。',
      properties: [
        {
          type: 'VideoModelResponseMetadata',
          parameters: [
            {
              name: 'timestamp',
              type: 'Date',
              description: '生成响应开始的时间戳。',
            },
            {
              name: 'modelId',
              type: 'string',
              description:
                '用于生成响应的响应模型的 ID。',
            },
            {
              name: 'headers',
              type: 'Record<string, string>',
              isOptional: true,
              description: '响应头。',
            },
            {
              name: 'providerMetadata',
              type: 'VideoModelProviderMetadata',
              isOptional: true,
              description:
                '此单次 API 调用的提供商特定元数据。当进行多次调用时，可用于访问每次调用的元数据。',
            },
          ],
        },
      ],
    },
  ]}
/>
