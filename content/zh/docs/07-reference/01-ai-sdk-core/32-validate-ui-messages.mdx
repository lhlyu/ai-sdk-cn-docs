---
title: validateUIMessages
description: validateUIMessages API 参考文档
---

# `validateUIMessages`

`validateUIMessages` 是一个异步函数，用于根据元数据、数据部分和工具的模式验证 UI 消息。它在处理或渲染消息数组之前确保类型安全和数据完整性。

## 基本用法

不使用自定义模式的简单验证：

```typescript
import { validateUIMessages } from 'ai';

const messages = [
  {
    id: '1',
    role: 'user',
    parts: [{ type: 'text', text: 'Hello!' }],
  },
];

const validatedMessages = await validateUIMessages({
  messages,
});
```

## 高级用法

使用自定义元数据、数据部分和工具的综合验证：

```typescript
import { validateUIMessages, tool } from 'ai';
import { z } from 'zod';

// 定义模式
const metadataSchema = z.object({
  timestamp: z.string().datetime(),
  userId: z.string(),
});

const dataSchemas = {
  chart: z.object({
    data: z.array(z.number()),
    labels: z.array(z.string()),
  }),
  image: z.object({
    url: z.string().url(),
    caption: z.string(),
  }),
};

const tools = {
  weather: tool({
    description: '获取天气信息',
    parameters: z.object({
      location: z.string(),
    }),
    execute: async ({ location }) => `${location}的天气：晴朗`,
  }),
};

// 包含自定义部分的消息
const messages = [
  {
    id: '1',
    role: 'user',
    metadata: { timestamp: '2024-01-01T00:00:00Z', userId: 'user123' },
    parts: [
      { type: 'text', text: '给我展示一个图表' },
      {
        type: 'data-chart',
        data: { data: [1, 2, 3], labels: ['A', 'B', 'C'] },
      },
    ],
  },
  {
    id: '2',
    role: 'assistant',
    parts: [
      {
        type: 'tool-weather',
        toolCallId: 'call_123',
        state: 'output-available',
        input: { location: '旧金山' },
        output: '旧金山的天气：晴朗',
      },
    ],
  },
];

// 使用所有模式进行验证
const validatedMessages = await validateUIMessages({
  messages,
  metadataSchema,
  dataSchemas,
  tools,
});
```
