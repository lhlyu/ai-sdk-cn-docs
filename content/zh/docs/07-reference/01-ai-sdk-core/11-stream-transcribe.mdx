---
title: "experimental_streamTranscribe"
description: "experimental_streamTranscribe 的 API 参考文档。"
---

# `experimental_streamTranscribe()`

<Note type="warning">
  `experimental_streamTranscribe` 是一个实验性功能。
</Note>

使用支持流式传输的转录模型，从实时原始音频中流式传输转录文本。

```ts
import { experimental_streamTranscribe as streamTranscribe } from 'ai';
import { openai } from '@ai-sdk/openai';

const result = streamTranscribe({
  model: openai.transcription('gpt-realtime-whisper'),
  audio: audioStream, // ReadableStream<Uint8Array | string>
  inputAudioFormat: { type: 'audio/pcm', rate: 24000 },
});

for await (const part of result.fullStream) {
  if (part.type === 'transcript-delta') {
    process.stdout.write(part.delta);
  }
}

console.log(await result.text);
```

## 导入

<Snippet
  text={`import { experimental_streamTranscribe as streamTranscribe } from "ai"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'model',
      type: 'TranscriptionModelV4',
      description:
        "要使用的转录模型。该模型必须支持流式传输 (`doStream`)。字符串模型 ID 通过全局 provider（默认是 AI Gateway）解析，该 provider 支持受支持模型的流式转录（例如 `openai/gpt-realtime-whisper`、`xai/grok-stt`）：`experimental_streamTranscribe({ model: 'openai/gpt-realtime-whisper', ... })`。",
    },
    {
      name: 'audio',
      type: 'ReadableStream<Uint8Array | string>',
      description:
        '要转录的原始音频块。`Uint8Array` 块包含原始音频字节；`string` 块包含 base64 编码的原始音频字节。',
    },
    {
      name: 'inputAudioFormat',
      type: '{ type: string; rate?: number }',
      description:
        '原始音频块的输入音频格式，例如 `{ type: "audio/pcm", rate: 24000 }`。支持的类型取决于具体 provider（例如 `audio/pcm`、`audio/pcmu`、`audio/pcma`）。',
    },
    {
      name: 'providerOptions',
      type: 'Record<string, JSONObject>',
      isOptional: true,
      description: '额外的 provider 特定选项。',
    },
    {
      name: 'abortSignal',
      type: 'AbortSignal',
      isOptional: true,
      description: '可选的终止信号，用于取消调用。',
    },
    {
      name: 'headers',
      type: 'Record<string, string>',
      isOptional: true,
      description:
        '额外的 HTTP/WebSocket 头部，如果 provider 支持的话。',
    },
    {
      name: 'includeRawChunks',
      type: 'boolean',
      isOptional: true,
      description:
        '当为 true 时，provider 会在流中包含原始 provider 块作为 `raw` 部分。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'fullStream',
      type: 'AsyncIterableStream<TranscriptionStreamPart>',
      description:
        '一个单消费者实时流，包含转录部分：`transcript-delta`、`transcript-partial`、`transcript-final`、`raw` 和 `error`。在需要同时获取流部分和最终结果时，在访问任何结果 promise 之前访问它一次。先访问结果 promise 会在内部消费流，并使 `fullStream` 不可用。',
    },
    {
      name: 'text',
      type: 'Promise<string>',
      description: '来自音频输入的完整转录文本。',
    },
    {
      name: 'segments',
      type: 'Promise<Array<{ text: string; startSecond: number; endSecond: number }>>',
      description:
        '包含时间信息的最终转录片段（如果可用）。',
    },
    {
      name: 'language',
      type: 'Promise<string | undefined>',
      description:
        '转录文本的语言，格式为 ISO-639-1（如果可用）。',
    },
    {
      name: 'durationInSeconds',
      type: 'Promise<number | undefined>',
      description: '转录文本的时长（秒），如果可用。',
    },
    {
      name: 'warnings',
      type: 'Promise<Warning[]>',
      description:
        '调用的警告，例如不支持的设置。当 provider 发出流开始信号时解析。',
    },
    {
      name: 'responses',
      type: 'Promise<Array<TranscriptionModelResponseMetadata>>',
      description: '响应元数据（时间戳、模型 ID、头部）。',
    },
    {
      name: 'providerMetadata',
      type: 'Promise<Record<string, JSONObject>>',
      description: '额外的 provider 特定元数据。',
    },
  ]}
/>

<Note>
  结果 promise 在流被消费时逐步确定。如果你提前停止消费 `fullStream`（例如通过 `break` 退出循环），底层 provider 连接将被关闭，未决的结果 promise 将被拒绝。
</Note>

## 线路格式（实验性）

通过 WebSocket 的流式转录使用 `@ai-sdk/provider-utils` 中定义的实验性转录流信封进行序列化（`experimental_parseTranscriptionStreamClientFrame`、`experimental_serializeTranscriptionStreamPart`、`experimental_parseTranscriptionStreamPart`）：客户端发送一个 `transcription-stream.start` TEXT 帧，音频作为 BINARY 帧发送，以及一个 `transcription-stream.audio-done` TEXT 帧；每个服务器 TEXT 帧是一个 JSON 序列化的转录流部分。AI Gateway 实现了此信封的服务器端。
