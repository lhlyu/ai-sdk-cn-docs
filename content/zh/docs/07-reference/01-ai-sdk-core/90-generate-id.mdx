---
title: generateId
description: 生成唯一标识符（API 参考）
---

# `generateId()`

生成唯一标识符。

这是 AI SDK 使用的相同 ID 生成器。

```ts
import { generateId } from 'ai';

const id = generateId();
```

## 导入

<Snippet text={`import { generateId } from "ai"`} prompt={false} />

## API 签名

### 返回值

表示所生成 ID 的字符串。

## 另请参阅

- [`createIdGenerator()`](/docs/reference/ai-sdk-core/create-id-generator)
