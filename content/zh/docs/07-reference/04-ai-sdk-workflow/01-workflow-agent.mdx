---
title: "WorkflowAgent"
description: "WorkflowAgent 类的 API 参考。"
---

# `WorkflowAgent`

创建一个持久、可恢复的 AI 智能体，用于工作流内部。`WorkflowAgent` 处理智能体循环、跨工作流步骤边界的工具 schema 序列化，以及内置的工具审批流程。

与 `ai` 包中的 [`ToolLoopAgent`](/docs/reference/ai-sdk-core/tool-loop-agent) 不同，`WorkflowAgent` 设计用于在进程重启后存活、暂停等待人工审批，并与 Workflow DevKit 的步骤机制集成。

`WorkflowAgent` 支持 `runtimeContext` 用于共享智能体状态，以及 `toolsContext` 用于每个工具的上下文。由于这些值可以跨越工作流和步骤边界，请保持它们为可序列化的持久数据。与 `ToolLoopAgent` 不同，不要在上下文中放置函数、类实例、符号、数据库客户端或 SDK 客户端；请传递标识符或配置，并在步骤函数内部重新创建不可序列化的资源。

```ts
import { WorkflowAgent } from '@ai-sdk/workflow';
import { tool } from 'ai';
import { z } from 'zod';

const agent = new WorkflowAgent({
  model: 'anthropic/claude-sonnet-4-6',
  instructions: '你是一个乐于助人的助手。',
  tools: {
    weather: tool({
      description: '获取某个地点的天气',
      inputSchema: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
      }),
    }),
  },
});

const result = await agent.stream({
  messages: [
    {
      role: 'user',
      content: [{ type: 'text', text: '纽约的天气怎么样？' }],
    },
  ],
});

console.log(result.messages);
```

要查看 `WorkflowAgent` 的实际应用，请查看[这些示例](#examples)。

## 导入

<Snippet
  text={`import { WorkflowAgent } from "@ai-sdk/workflow"`}
  prompt={false}
/>

## 构造函数

### 参数

<PropertiesTable
  content={[
    {
      name: 'id',
      type: 'string',
      isOptional: true,
      description: '智能体的 ID。',
    },
    {
      name: 'model',
      type: 'LanguageModel',
      isRequired: true,
      description:
        "要使用的语言模型。可以是与 Vercel AI Gateway 兼容的字符串（例如 'anthropic/claude-sonnet-4-6'）或提供商实例（例如 `openai('gpt-4o')`）。",
    },
    {
      name: 'instructions',
      type: 'Instructions',
      isOptional: true,
      description:
        '智能体的指令，用作系统提示词。使用 SystemModelMessage 形式时支持提供商特定选项（例如缓存）。',
    },
    {
      name: 'tools',
      type: 'Record<string, Tool>',
      isOptional: true,
      description:
        '智能体可以调用的一组工具。键是工具名称。工具会跨工作流步骤边界序列化为 JSON Schema，并在运行时使用 Ajv 进行验证。',
    },
    {
      name: 'toolChoice',
      type: 'ToolChoice',
      isOptional: true,
      description:
        "工具调用选择策略。选项：'auto' | 'none' | 'required' | { type: 'tool', toolName: string }。默认值：'auto'。",
    },
    {
      name: 'stopWhen',
      type: 'StopCondition | StopCondition[]',
      isOptional: true,
      description:
        '智能体循环的默认停止条件。每次流式传输的值会覆盖此默认值。使用 `isLoopFinished()` 让智能体运行直到所有工具调用完成，但要注意潜在的失控循环。参见 https://ai-sdk.dev/v7/docs/reference/ai-sdk-core/loop-finished#isloopfinished。',
    },
    {
      name: 'activeTools',
      type: 'ActiveTools<TTools>',
      isOptional: true,
      description:
        '默认的可用工具集。限制模型可以调用的工具，而不改变工具调用和结果类型。每次流式传输的值会覆盖此默认值。',
    },
    {
      name: 'output',
      type: 'OutputSpecification',
      isOptional: true,
      description:
        '默认的结构化输出规范。每次流式传输的值会覆盖此默认值。',
    },
    {
      name: 'repairToolCall',
      type: 'ToolCallRepairFunction',
      isOptional: true,
      description:
        '修复无法解析的工具调用的默认函数。每次流式传输的值会覆盖此默认值。',
    },
    {
      name: 'experimental_download',
      type: 'DownloadFunction',
      isOptional: true,
      description:
        '用于 URL 的默认自定义下载函数。每次流式传输的值会覆盖此默认值。',
    },
    {
      name: 'experimental_sandbox',
      type: 'Experimental_SandboxSession',
      isOptional: true,
      description:
        '默认沙箱会话，作为 `experimental_sandbox` 传递给工具描述和执行，并暴露给 `prepareStep`。每次流式传输的值会覆盖此默认值。',
    },
    {
      name: 'prepareStep',
      type: 'PrepareStepCallback',
      isOptional: true,
      description:
        '在智能体循环中每个步骤之前调用的回调。用于修改设置、管理上下文、动态注入消息，或覆盖当前步骤的 `experimental_sandbox`。接收步骤编号、之前的步骤、消息、上下文和沙箱。',
    },
    {
      name: 'prepareCall',
      type: 'PrepareCallCallback',
      isOptional: true,
      description:
        '在智能体循环开始前调用一次的回调。用于根据运行时上下文转换模型、指令、工具配置或其他设置。不能覆盖 `tools`（在构造时绑定以保证类型安全）。',
    },
    {
      name: 'runtimeContext',
      type: 'Context',
      isOptional: true,
      description:
        '每次流式传输调用的默认共享运行时上下文。流经 `prepareStep`、生命周期回调和步骤结果。每次流式传输的值会覆盖此默认值。在工作流中使用时必须可序列化。',
    },
    {
      name: 'toolsContext',
      type: 'InferToolSetContext<TTools>',
      isOptional: true,
      description:
        '每次流式传输调用的默认每工具上下文映射。每个工具只接收自己经过验证的条目作为 `context`。每次流式传输的值会覆盖此默认值。在工作流中使用时必须可序列化。',
    },
    {
      name: 'telemetry',
      type: 'TelemetryOptions',
      isOptional: true,
      description:
        '遥测配置，包含启用/禁用遥测、设置函数 ID 以及记录输入/输出的选项。',
    },
    {
      name: 'experimental_onStart',
      type: 'WorkflowAgentOnStartCallback',
      isOptional: true,
      description:
        '智能体开始流式传输时、任何 LLM 调用之前调用的回调。接收模型、消息、运行时上下文和工具上下文。如果在 `stream()` 中也指定了，两个回调都会触发（构造函数优先）。实验性（可能在补丁版本中破坏）。',
      properties: [
        {
          type: 'GenerateTextStartEvent',
          parameters: [
            {
              name: 'model',
              type: 'LanguageModel',
              description: '用于生成内容的模型。',
            },
            {
              name: 'messages',
              type: 'Array<ModelMessage>',
              description: '发送给模型的消息。',
            },
            {
              name: 'runtimeContext',
              type: 'Context',
              description: '智能体循环的共享运行时上下文。',
            },
            {
              name: 'toolsContext',
              type: 'InferToolSetContext<Tools>',
              description: '智能体循环的每工具上下文映射。',
            },
          ],
        },
      ],
    },
    {
      name: 'experimental_onStepStart',
      type: 'WorkflowAgentOnStepStartCallback',
      isOptional: true,
      description:
        '每个步骤（LLM 调用）开始之前调用的回调。接收步骤编号、模型、消息、之前的步骤、运行时上下文和工具上下文。如果在 `stream()` 中也指定了，两个回调都会触发（构造函数优先）。实验性（可能在补丁版本中破坏）。',
      properties: [
        {
          type: 'GenerateTextStepStartEvent',
          parameters: [
            {
              name: 'model',
              type: 'LanguageModel',
              description: '用于此步骤的模型。',
            },
            {
              name: 'messages',
              type: 'Array<ModelMessage>',
              description: '将为此步骤发送给模型的消息。',
            },
            {
              name: 'steps',
              type: 'ReadonlyArray<StepResult>',
              description: '所有先前已完成步骤的结果。',
            },
            {
              name: 'runtimeContext',
              type: 'Context',
              description: '此步骤的共享运行时上下文。',
            },
            {
              name: 'toolsContext',
              type: 'InferToolSetContext<Tools>',
              description: '此步骤的每工具上下文映射。',
            },
          ],
        },
      ],
    },
    {
      name: 'onToolExecutionStart',
      type: 'WorkflowAgentonToolExecutionStartCallback',
      isOptional: true,
      description:
        "在工具的 execute 函数运行之前调用的回调。如果在 `stream()` 中也指定了，两个回调都会触发（构造函数优先）。实验性（可能在补丁版本中破坏）。",
      properties: [
        {
          type: 'ToolExecutionStartEvent',
          parameters: [
            {
              name: 'callId',
              type: 'string',
              description:
                '此生成调用的唯一标识符，用于关联事件。',
            },
            {
              name: 'toolCall',
              type: '{ type: "tool-call"; toolCallId: string; toolName: string; input: unknown }',
              description: '正在执行的工具调用。',
            },
            {
              name: 'messages',
              type: 'Array<ModelMessage>',
              description:
                '发送给语言模型以发起包含该工具调用的响应的消息。',
            },
            {
              name: 'toolContext',
              type: 'InferToolContext<TOOLS[toolName]>',
              description:
                '即将执行的工具调用的工具特定上下文对象。已缩小到单个工具的上下文类型，而不是整个工具集。',
            },
          ],
        },
      ],
    },
    {
      name: 'onToolExecutionEnd',
      type: 'WorkflowAgentonToolExecutionEndCallback',
      isOptional: true,
      description:
        "在工具的 execute 函数完成或出错后立即调用的回调。`toolOutput` 字段是一个可判别联合：检查 `toolOutput.type` 以确定结果是 `'tool-result'` 还是 `'tool-error'`。如果在 `stream()` 中也指定了，两个回调都会触发（构造函数优先）。实验性（可能在补丁版本中破坏）。",
      properties: [
        {
          type: 'ToolExecutionEndEvent',
          parameters: [
            {
              name: 'callId',
              type: 'string',
              description:
                '此生成调用的唯一标识符，用于关联事件。',
            },
            {
              name: 'toolCall',
              type: '{ type: "tool-call"; toolCallId: string; toolName: string; input: unknown }',
              description: '已执行的工具调用。',
            },
            {
              name: 'durationMs',
              type: 'number',
              description:
                '工具执行时间（毫秒）。工作流智能体使用 `durationMs`；AI SDK Core 生成回调使用 `toolExecutionMs`。',
            },
            {
              name: 'messages',
              type: 'Array<ModelMessage>',
              description:
                '发送给语言模型以发起包含该工具调用的响应的消息。',
            },
            {
              name: 'toolContext',
              type: 'InferToolContext<TOOLS[toolName]>',
              description:
                '刚刚完成的工具调用的工具特定上下文对象。已缩小到单个工具的上下文类型，而不是整个工具集。',
            },
            {
              name: 'toolOutput',
              type: 'ToolOutput<TOOLS>',
              description:
                "表示工具执行结果的可判别联合。当 `type` 为 `'tool-result'` 时，`output` 字段包含工具的返回值。当 `type` 为 `'tool-error'` 时，`error` 字段包含错误。",
            },
          ],
        },
      ],
    },
    {
      name: 'onStepEnd',
      type: 'WorkflowAgentOnStepEndCallback',
      isOptional: true,
      description:
        '每个智能体步骤完成后调用的回调。如果在 `stream()` 中也指定了，两个回调都会触发（构造函数优先）。',
    },
    {
      name: 'onStepFinish',
      type: 'WorkflowAgentOnStepFinishCallback',
      isOptional: true,
      description:
        '已弃用。请改用 `onStepEnd`。此别名仅在未提供 `onStepEnd` 时作为回退使用。',
    },
    {
      name: 'onEnd',
      type: 'WorkflowAgentOnEndCallback',
      isOptional: true,
      description:
        '所有智能体步骤完成且响应完整时调用的回调。接收步骤、消息、文本、完成原因、总用量和上下文。如果在 `stream()` 中也指定了，两个回调都会触发（构造函数优先）。',
    },
    {
      name: 'maxOutputTokens',
      type: 'number',
      isOptional: true,
      description: '模型允许生成的最大 token 数。',
    },
    {
      name: 'temperature',
      type: 'number',
      isOptional: true,
      description: '采样温度，控制随机性。',
    },
    {
      name: 'topP',
      type: 'number',
      isOptional: true,
      description: 'Top-p（核）采样参数。',
    },
    {
      name: 'topK',
      type: 'number',
      isOptional: true,
      description: 'Top-k 采样参数。',
    },
    {
      name: 'presencePenalty',
      type: 'number',
      isOptional: true,
      description: '存在惩罚参数。',
    },
    {
      name: 'frequencyPenalty',
      type: 'number',
      isOptional: true,
      description: '频率惩罚参数。',
    },
    {
      name: 'stopSequences',
      type: 'string[]',
      isOptional: true,
      description: '停止模型输出的自定义 token 序列。',
    },
    {
      name: 'seed',
      type: 'number',
      isOptional: true,
      description: '用于确定性生成的种子（如果支持）。',
    },
    {
      name: 'maxRetries',
      type: 'number',
      isOptional: true,
      description:
        '可重试的模型调用失败的重试次数。设置为 0 以禁用重试。会尊重 Retry-After 响应头，且持久工作流步骤重试不会叠加。默认值：2。',
    },
    {
      name: 'headers',
      type: 'Record<string, string | undefined>',
      isOptional: true,
      description:
        '随请求发送的附加 HTTP 头。仅适用于基于 HTTP 的提供商。',
    },
    {
      name: 'providerOptions',
      type: 'ProviderOptions',
      isOptional: true,
      description: '附加的提供商特定配置。',
    },
  ]}
/>

## 属性

<PropertiesTable
  content={[
    {
      name: 'id',
      type: 'string | undefined',
      description:
        '智能体的 ID。用于遥测标识。只读。',
    },
    {
      name: 'tools',
      type: 'Record<string, Tool>',
      description: '为此智能体配置的工具集。只读。',
    },
  ]}
/>

## 方法

### `stream()`

运行智能体循环，流式传输响应并根据需要执行工具调用。返回一个解析为 `WorkflowAgentStreamResult` 的 Promise。

```ts
const result = await agent.stream({
  messages: [{ role: 'user', content: [{ type: 'text', text: '你好' }] }],
});
```

<PropertiesTable
  content={[
    {
      name: 'prompt',
      type: 'string | Array<ModelMessage>',
      description: '提示词字符串或消息列表。可以使用 `prompt` 或 `messages`，但不能同时使用。',
    },
    {
      name: 'messages',
      type: 'Array<ModelMessage>',
      description: '要处理的对话消息。可以使用 `prompt` 或 `messages`，但不能同时使用。',
    },
    {
      name: 'writable',
      type: 'WritableStream<ModelCallStreamPart>',
      isOptional: true,
      description:
        '实时接收原始模型流式传输部分的可写流。使用 `createModelCallToUIChunkTransform()` 在响应边界转换为 UI 消息块。',
    },
    {
      name: 'instructions',
      type: 'Instructions',
      isOptional: true,
      description: '覆盖此次调用的智能体指令。',
    },
    {
      name: 'system',
      type: 'string',
      isOptional: true,
      description: '已弃用。请改用 `instructions`。',
    },
    {
      name: 'stopWhen',
      type: 'StopCondition | StopCondition[]',
      isOptional: true,
      description: '结束智能体循环的条件。使用 `isLoopFinished()` 让智能体运行直到所有工具调用完成，但要注意潜在的失控循环。参见 https://ai-sdk.dev/v7/docs/reference/ai-sdk-core/loop-finished#isloopfinished。',
    },

    {
      name: 'toolChoice',
      type: 'ToolChoice',
      isOptional: true,
      description: "覆盖此次调用的工具选择策略。默认值：'auto'。",
    },
    {
      name: 'activeTools',
      type: 'ActiveTools<TTools>',
      isOptional: true,
      description: '限制此次调用可用的工具子集，而不改变工具调用和结果类型。',
    },
    {
      name: 'output',
      type: 'OutputSpecification',
      isOptional: true,
      description:
        '结构化输出规范。使用 `Output.object({ schema })` 获取类型化对象，或使用 `Output.text()` 获取文本。',
    },
    {
      name: 'timeout',
      type: 'number',
      isOptional: true,
      description: '超时时间（毫秒）。创建一个 AbortSignal，在给定时间后中止操作。',
    },
    {
      name: 'sendFinish',
      type: 'boolean',
      isOptional: true,
      description: "流式传输完成时是否向可写流发送 'finish' 块。默认值：true。",
    },
    {
      name: 'preventClose',
      type: 'boolean',
      isOptional: true,
      description: '流式传输完成后是否阻止关闭可写流。默认值：false。',
    },
    {
      name: 'includeRawChunks',
      type: 'boolean',
      isOptional: true,
      description: '在流中是否包含来自提供商的原始未处理块。默认值：false。',
    },
    {
      name: 'repairToolCall',
      type: 'ToolCallRepairFunction',
      isOptional: true,
      description: '当工具调用无法解析时尝试自动恢复的回调。',
    },
    {
      name: 'experimental_transform',
      type: 'StreamTextTransform | Array<StreamTextTransform>',
      isOptional: true,
      description: '按顺序应用的流转换。必须保持流结构。',
    },
    {
      name: 'experimental_download',
      type: 'DownloadFunction',
      isOptional: true,
      description: '用于获取文件/URL 的自定义下载函数。',
    },
    {
      name: 'experimental_sandbox',
      type: 'Experimental_SandboxSession',
      isOptional: true,
      description:
        '沙箱会话，作为 `experimental_sandbox` 传递给工具描述和执行，并暴露给 `prepareStep`。覆盖构造函数默认值。',
    },
    {
      name: 'telemetry',
      type: 'TelemetryOptions',
      isOptional: true,
      description: '每次调用的遥测配置。',
    },
    {
      name: 'runtimeContext',
      type: 'Context',
      isOptional: true,
      description:
        '此次流式传输调用的共享运行时上下文。覆盖构造函数默认值，并流经 `prepareStep`、生命周期回调和步骤结果。在工作流中使用时必须可序列化。',
    },
    {
      name: 'toolsContext',
      type: 'InferToolSetContext<TTools>',
      isOptional: true,
      description:
        '此次流式传输调用的每工具上下文映射。覆盖构造函数默认值。每个工具只接收自己经过验证的条目作为 `context`。在工作流中使用时必须可序列化。',
    },
    {
      name: 'prepareStep',
      type: 'PrepareStepCallback',
      isOptional: true,
      description:
        '每次调用的 prepareStep 覆盖。接收初始指令和消息以及当前步骤状态。',
    },
    {
      name: 'experimental_onStart',
      type: 'WorkflowAgentOnStartCallback',
      isOptional: true,
      description:
        '每次调用的 onStart 回调。如果在构造函数中也指定了，两个都会触发（构造函数优先）。实验性。',
    },
    {
      name: 'experimental_onStepStart',
      type: 'WorkflowAgentOnStepStartCallback',
      isOptional: true,
      description:
        '每次调用的 onStepStart 回调。如果在构造函数中也指定了，两个都会触发（构造函数优先）。实验性。',
    },
    {
      name: 'onToolExecutionStart',
      type: 'WorkflowAgentonToolExecutionStartCallback',
      isOptional: true,
      description:
        '每次调用的 onToolExecutionStart 回调。如果在构造函数中也指定了，两个都会触发（构造函数优先）。',
    },
    {
      name: 'onToolExecutionEnd',
      type: 'WorkflowAgentonToolExecutionEndCallback',
      isOptional: true,
      description:
        '每次调用的 onToolExecutionEnd 回调。如果在构造函数中也指定了，两个都会触发（构造函数优先）。',
    },
    {
      name: 'onStepEnd',
      type: 'WorkflowAgentOnStepEndCallback',
      isOptional: true,
      description:
        '每次调用的 onStepEnd 回调。如果在构造函数中也指定了，两个都会触发（构造函数优先）。',
    },
    {
      name: 'onStepFinish',
      type: 'WorkflowAgentOnStepFinishCallback',
      isOptional: true,
      description:
        '已弃用。请改用 `onStepEnd`。此别名仅在未提供 `onStepEnd` 时作为回退使用。',
    },
    {
      name: 'onEnd',
      type: 'WorkflowAgentOnEndCallback',
      isOptional: true,
      description:
        '每次调用的 onEnd 回调。如果在构造函数中也指定了，两个都会触发（构造函数优先）。',
    },
    {
      name: 'onError',
      type: 'WorkflowAgentOnErrorCallback',
      isOptional: true,
      description: '流式传输过程中发生错误时调用的回调。',
    },
    {
      name: 'onAbort',
      type: 'WorkflowAgentOnAbortCallback',
      isOptional: true,
      description: '操作被中止时调用的回调。接收所有先前已完成的步骤。',
    },

]}
/>

#### 返回值

返回一个 `Promise<WorkflowAgentStreamResult>`，包含以下属性：

<PropertiesTable
  content={[
    {
      name: 'messages',
      type: 'Array<ModelMessage>',
      description: '最终消息，包括所有工具调用和结果。',
    },
    {
      name: 'steps',
      type: 'Array<StepResult>',
      description: '智能体执行的所有步骤的详细信息。',
    },
    {
      name: 'toolCalls',
      type: 'Array<ToolCall>',
      description:
        '最后一步的工具调用，包括未执行的调用（例如需要审批的工具）。',
    },
    {
      name: 'toolResults',
      type: 'Array<ToolResult>',
      description:
        '最后一步的工具结果。仅包含已执行工具的结果。',
    },
    {
      name: 'error',
      type: 'unknown | undefined',
      description:
        "模型流错误部分的原始值。当发出错误部分时该属性存在，即使其值为 undefined；使用 `'error' in result` 来区分这种情况。",
    },
    {
      name: 'output',
      type: 'OUTPUT',
      description:
        '如果提供了 `output` 规范，则为结构化输出。',
    },
  ]}
/>

## 工具函数

### `createModelCallToUIChunkTransform(options?)`

创建一个 `TransformStream`，将原始的 `ModelCallStreamPart` 块（智能体写入 `writable` 流）转换为适合客户端消费的 `UIMessageChunk` 对象。

```ts
import { createModelCallToUIChunkTransform } from '@ai-sdk/workflow';

return createUIMessageStreamResponse({
  stream: run.readable.pipeThrough(createModelCallToUIChunkTransform()),
});
```

当使用 `WorkflowChatTransport` 游标恢复时，从索引 `0` 重放原始工作流流，并将非负的 UI 块索引传递给转换器：

```ts
const readable = run
  .getReadable({ startIndex: 0 })
  .pipeThrough(createModelCallToUIChunkTransform({ uiStartIndex: startIndex }));
```

`uiStartIndex` 必须是非负安全整数。原始模型流部分和 UI 消息块不是一一对应的，因此不要将 UI 块索引传递给 `getReadable`。负尾部索引需要已经存储 `UIMessageChunk` 对象的持久流。

### `toUIMessageChunk()`

将单个 `ModelCallStreamPart` 转换为 `UIMessageChunk`。对于不映射到 UI 块的部分返回 `undefined`。

```ts
import { toUIMessageChunk } from '@ai-sdk/workflow';

const uiChunk = toUIMessageChunk(modelCallPart);
```

## 类型

### `ActiveTools`

```ts
type ActiveTools<TTools extends ToolSet> =
  | ReadonlyArray<keyof TTools & string>
  | undefined;
```

将工作流智能体调用限制为列出的工具名称。`undefined` 表示不应用工具限制。

### `InferWorkflowAgentUIMessage`

推断 `WorkflowAgent` 实例的 UI 消息类型。可选地接受第二个类型参数用于自定义消息元数据。

```ts
import { WorkflowAgent, InferWorkflowAgentUIMessage } from '@ai-sdk/workflow';

const agent = new WorkflowAgent({
  model: 'anthropic/claude-sonnet-4-6',
  tools: { weather: weatherTool },
});

type MyAgentUIMessage = InferWorkflowAgentUIMessage<typeof agent>;
```

### `InferWorkflowAgentTools`

推断 `WorkflowAgent` 实例的工具集类型。

```ts
import { WorkflowAgent, InferWorkflowAgentTools } from '@ai-sdk/workflow';

type MyTools = InferWorkflowAgentTools<typeof myAgent>;
```

## 示例

### 带工具的基础智能体

```ts
import { WorkflowAgent } from '@ai-sdk/workflow';
import { tool } from 'ai';
import { z } from 'zod';

const agent = new WorkflowAgent({
  model: 'anthropic/claude-sonnet-4-6',
  instructions: '你是一个乐于助人的助手。',
  tools: {
    weather: tool({
      description: '获取某个地点的天气',
      inputSchema: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
        condition: 'sunny',
      }),
    }),
  },
});

const result = await agent.stream({
  messages: [
    {
      role: 'user',
      content: [{ type: 'text', text: '纽约的天气怎么样？' }],
    },
  ],
});

console.log(result.messages);
console.log(result.steps);
```

### 工作流中使用持久工具的智能体

```ts filename="workflow/agent-chat.ts"
import { WorkflowAgent, type ModelCallStreamPart } from '@ai-sdk/workflow';
import { convertToModelMessages, tool, type UIMessage } from 'ai';
import { getWritable } from 'workflow';
import { z } from 'zod';

// 标记为 'use step' 的工具 execute 函数成为持久工作流步骤
// 具有自动重试和持久化
async function searchFlightsStep(input: {
  origin: string;
  destination: string;
}) {
  'use step';
  const response = await fetch(`https://api.flights.example/search?...`);
  return response.json();
}

export async function chat(messages: UIMessage[]) {
  'use workflow';

  const modelMessages = await convertToModelMessages(messages);

  const agent = new WorkflowAgent({
    model: 'anthropic/claude-sonnet-4-6',
    instructions: '你是一个航班预订助手。',
    tools: {
      searchFlights: tool({
        description: '搜索可用航班',
        inputSchema: z.object({
          origin: z.string(),
          destination: z.string(),
        }),
        execute: searchFlightsStep,
      }),
    },
  });

  const result = await agent.stream({
    messages: modelMessages,
    writable: getWritable<ModelCallStreamPart>(),
  });

  return { messages: result.messages };
}
```

```ts filename="app/api/chat/route.ts"
import { createModelCallToUIChunkTransform } from '@ai-sdk/workflow';
import { createUIMessageStreamResponse, type UIMessage } from 'ai';
import { start } from 'workflow/api';
import { chat } from '@/workflow/agent-chat';

export async function POST(request: Request) {
  const { messages }: { messages: UIMessage[] } = await request.json();

  const run = await start(chat, [messages]);

  return createUIMessageStreamResponse({
    stream: run.readable.pipeThrough(createModelCallToUIChunkTransform()),
  });
}
```

### 带结构化输出的智能体

```ts
import { WorkflowAgent, Output } from '@ai-sdk/workflow';
import { z } from 'zod';

const analysisAgent = new WorkflowAgent({
  model: 'anthropic/claude-sonnet-4-6',
});

const result = await analysisAgent.stream({
  messages: [
    {
      role: 'user',
      content: [
        {
          type: 'text',
          text: '分析："这个产品超出了我的预期！"',
        },
      ],
    },
  ],
  output: Output.object({
    schema: z.object({
      sentiment: z.enum(['positive', 'negative', 'neutral']),
      score: z.number(),
      summary: z.string(),
    }),
  }),
});

console.log(result.output);
// { sentiment: 'positive', score: 9, summary: '...' }
```

### 带工具审批的智能体

对于 `WorkflowAgent`，工具审批在工具定义上通过 `needsApproval` 配置。对于 `generateText`、`streamText` 和 `ToolLoopAgent`，请改用 `toolApproval`。

```ts
import { WorkflowAgent } from '@ai-sdk/workflow';
import { tool } from 'ai';
import { z } from 'zod';

const agent = new WorkflowAgent({
  model: 'anthropic/claude-sonnet-4-6',
  tools: {
    bookFlight: tool({
      description: '预订航班',
      inputSchema: z.object({
        flightId: z.string(),
        passengerName: z.string(),
      }),
      needsApproval: true, // 暂停智能体直到用户批准
      execute: bookFlightStep,
    }),
  },
});
```

### 带生命周期回调的智能体

```ts
import { WorkflowAgent } from '@ai-sdk/workflow';

const agent = new WorkflowAgent({
  model: 'anthropic/claude-sonnet-4-6',
  tools: { weather: weatherTool },

  // 智能体级回调
  onStepEnd({ usage }) {
    console.log('使用的 token 数：', usage.totalTokens);
  },
});

const result = await agent.stream({
  messages,

  // 每次调用回调（两者都会触发）
  onStepEnd({ usage }) {
    await trackUsage(usage);
  },

  onEnd({ steps, totalUsage }) {
    console.log(
      `在 ${steps.length} 个步骤中完成，共 ${totalUsage.totalTokens} 个 token`,
    );
  },
});
```
