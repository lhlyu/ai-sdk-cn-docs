---
title: Azure AI
description: 了解如何在 AI SDK 中使用 @quail-ai/azure-ai-provider。
---

# AI SDK 的 Azure 自定义提供商

**[Quail-AI/azure-ai-provider](https://github.com/QuailAI/azure-ai-provider)** 实现了与 Azure 托管语言模型的非官方集成，这些模型使用 Azure 原生 API 而非标准 OpenAI API 格式。

## 语言模型

该提供商适用于 Azure AI Foundry 中任何兼容 Azure-Rest AI-inference API 的模型。

### 已测试模型：

- DeepSeek-R1
- LLama 3.3-70B Instruct
- Cohere-command-r-08-2024

## 设置

### 安装

通过 npm 安装提供商：

```bash
npm i @quail-ai/azure-ai-provider
```

## 提供商实例

创建 Azure AI 资源并配置端点 URL 和 API 密钥。将以下内容添加到您的 `.env` 文件中：

```bash
AZURE_API_ENDPOINT=https://<your-resource>.services.ai.azure.com/models
AZURE_API_KEY=<your-api-key>
```

从包中导入 `createAzure` 以创建提供商实例：

```ts
import { createAzure } from '@quail-ai/azure-ai-provider';

const azure = createAzure({
  endpoint: process.env.AZURE_API_ENDPOINT,
  apiKey: process.env.AZURE_API_KEY,
});
```

## 基本用法

使用 Azure 自定义提供商生成文本：

```ts
import { generateText } from 'ai';

const { text } = await generateText({
  model: azure('your-deployment-name'),
  prompt: '写一个关于机器人的故事。',
});
```

## 状态

> ✅ 聊天补全：支持流式传输和非流式传输响应\
> ⚠️ 工具调用：功能高度依赖模型选择\
> ⚠️ 嵌入：已实现但未测试
