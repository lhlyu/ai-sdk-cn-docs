---
title: Maxim
description: 使用 Maxim 评估与观测 LLM 应用
---

# Maxim 可观测性

[Maxim AI](https://getmaxim.ai) 通过将传统软件最佳实践应用于非确定性 AI 工作流，简化了 AI 应用的开发和部署。我们的评估与可观测性工具帮助团队在整个 AI 应用生命周期中保持质量、可靠性和速度。Maxim 与 AI SDK 集成，提供以下功能：

- **自动可观测性** – 通过简单的包装器，为 AI SDK 调用添加追踪、日志记录和元数据。

- **统一模型包装** – 统一支持 OpenAI、Anthropic、Google 等模型。

- **自定义元数据与标签** – 支持附加追踪名称、标签和会话 ID 以跟踪使用情况。

- **流式传输与结构化输出支持** – 无缝处理流式响应和结构化输出。

# 使用 AI SDK 设置 Maxim

## 环境要求

```
"ai"
"@ai-sdk/openai"
"@ai-sdk/anthropic"
"@ai-sdk/google"
"@maximai/maxim-js"
```

## 环境变量

```
MAXIM_API_KEY=
MAXIM_LOG_REPO_ID=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
```

## 初始化日志记录器

```javascript
import { Maxim } from '@maximai/maxim-js';

async function initializeMaxim() {
  const apiKey = process.env.MAXIM_API_KEY || '';
  if (!apiKey) {
    throw new Error(
      '环境变量中未定义 MAXIM_API_KEY',
    );
  }

  const maxim = new Maxim({ apiKey });
  const logger = await maxim.logger({
    id: process.env.MAXIM_LOG_REPO_ID || '',
  });

  if (!logger) {
    throw new Error('日志记录器不可用');
  }

  return { maxim, logger };
}
```

## 使用 Maxim 包装 AI SDK 模型

```javascript
import { openai } from '@ai-sdk/openai';
import { wrapMaximAISDKModel } from '@maximai/maxim-js/vercel-ai-sdk';

const model = wrapMaximAISDKModel(openai('gpt-5'), logger);
```

## 使用包装后的模型进行 LLM 调用

```javascript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { wrapMaximAISDKModel } from '@maximai/maxim-js/vercel-ai-sdk';

const model = wrapMaximAISDKModel(openai('gpt-5'), logger);

// 生成文本并自动记录日志
const response = await generateText({
  model: model,
  prompt: '写一首关于编程中递归的俳句。',
  temperature: 0.8,
  system: '你是一个乐于助人的助手。',
});

console.log('响应:', response.text);
```

## 使用不同的 AI SDK 函数

包装后的模型可与所有 Vercel AI SDK 函数无缝协作：

### **结构化输出**

```javascript
import { generateText, Output } from 'ai';
import { z } from 'zod';

const response = await generateText({
  model: model,
  prompt: '为 John Doe 生成一个用户档案',
  output: Output.object({
    schema: z.object({
      name: z.string(),
      age: z.number(),
      email: z.string().email(),
      interests: z.array(z.string()),
    }),
  }),
});

console.log(response.output);
```

### **流式文本**

```javascript
import { streamText } from 'ai';

const { textStream } = await streamText({
  model: model,
  prompt: '写一个关于太空探索的短篇故事',
  system: '你是一个创意作家',
});

for await (const textPart of textStream) {
  process.stdout.write(textPart);
}
```

## 自定义元数据与追踪

### **使用自定义元数据**

```javascript
import { MaximVercelProviderMetadata } from '@maximai/maxim-js/vercel-ai-sdk';

const response = await generateText({
  model: model,
  prompt: '你好，最近怎么样？',
  providerOptions: {
    maxim: {
      traceName: 'custom-trace-name',
      traceTags: {
        type: 'demo',
        priority: 'high',
      },
    } as MaximVercelProviderMetadata,
  },
});
```

### **可用元数据字段**

**实体命名：**

- `sessionName` - 覆盖默认会话名称
- `traceName` - 覆盖默认追踪名称
- `spanName` - 覆盖默认跨度名称
- `generationName` - 覆盖默认 LLM 生成名称

**实体标签：**

- `sessionTags` - 为会话添加自定义标签 `(对象: {键: 值})`
- `traceTags` - 为追踪添加自定义标签 `(对象: {键: 值})`
- `spanTags` - 为跨度添加自定义标签 `(对象: {键: 值})`
- `generationTags` - 为 LLM 生成添加自定义标签 `(对象: {键: 值})`

**ID 引用：**

- `sessionId` - 将此追踪链接到现有会话
- `traceId` - 使用特定的追踪 ID
- `spanId` - 使用特定的跨度 ID

![Maxim 演示](https://cdn.getmaxim.ai/public/images/maxim_vercel.gif)

## 流式传输支持

```javascript
import { streamText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { wrapMaximAISDKModel, MaximVercelProviderMetadata } from '@maximai/maxim-js/vercel-ai-sdk';

const model = wrapMaximAISDKModel(openai('gpt-5'), logger);

const { textStream } = await streamText({
  model: model,
  prompt: '写一个关于机器人学习绘画的故事。',
  system: '你是一个创意故事讲述者',
  providerOptions: {
    maxim: {
      traceName: '故事生成',
      traceTags: {
        type: 'creative',
        format: 'streaming'
      },
    } as MaximVercelProviderMetadata,
  },
});

for await (const textPart of textStream) {
  process.stdout.write(textPart);
}
```

## 多提供商支持

```javascript
import { openai } from '@ai-sdk/openai';
import { anthropic } from '@ai-sdk/anthropic';
import { google } from '@ai-sdk/google';
import { wrapMaximAISDKModel } from '@maximai/maxim-js/vercel-ai-sdk';

// 包装不同提供商的模型
const openaiModel = wrapMaximAISDKModel(openai('gpt-5'), logger);
const anthropicModel = wrapMaximAISDKModel(
  anthropic('claude-3-5-sonnet-20241022'),
  logger,
);
const googleModel = wrapMaximAISDKModel(google('gemini-pro'), logger);

// 使用相同的接口调用它们
const responses = await Promise.all([
  generateText({ model: openaiModel, prompt: '来自 OpenAI 的问候' }),
  generateText({ model: anthropicModel, prompt: '来自 Anthropic 的问候' }),
  generateText({ model: googleModel, prompt: '来自 Google 的问候' }),
]);
```

## Next.js 集成

### **API 路由示例**

```javascript
// app/api/chat/route.js
import { streamText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { wrapMaximAISDKModel, MaximVercelProviderMetadata } from '@maximai/maxim-js/vercel-ai-sdk';
import { Maxim } from "@maximai/maxim-js";

const maxim = new Maxim({ apiKey });
const logger = await maxim.logger({ id: process.env.MAXIM_LOG_REPO_ID });
const model = wrapMaximAISDKModel(openai('gpt-5'), logger);

export async function POST(req) {
  const { messages } = await req.json();

  const result = await streamText({
    model: model,
    messages,
    system: '你是一个乐于助人的助手',
    providerOptions: {
      maxim: {
        traceName: '聊天 API',
        traceTags: {
          endpoint: '/api/chat',
          type: 'conversation'
        },
      } as MaximVercelProviderMetadata,
    },
  });

  return result.toAIStreamResponse();
}
```

### **客户端集成**

```javascript
// components/Chat.jsx
import { useChat } from 'ai/react';

export default function Chat() {
  const { messages, input, handleInputChange, handleSubmit } = useChat({
    api: '/api/chat',
  });

  return (
    <div>
      {messages.map(m => (
        <div key={m.id}>
          <strong>{m.role}:</strong> {m.content}
        </div>
      ))}

      <form onSubmit={handleSubmit}>
        <input
          value={input}
          onChange={handleInputChange}
          placeholder="说点什么..."
        />
        <button type="submit">发送</button>
      </form>
    </div>
  );
}
```

## 了解更多

- 为 Vercel AI SDK 设置 Maxim 追踪后，您可以探索 Maxim 平台的其他功能：
  - **提示词管理**：跨环境和智能体版本管理、管理并动态应用提示词。
  - **评估**：对追踪、生成和完整智能体轨迹运行自动化和手动评估。
  - **模拟**：通过模拟的多轮交互和工作流，在真实场景中测试智能体。

更多详情，请查看 Vercel AI SDK 的 [Maxim 集成文档](https://www.getmaxim.ai/docs/sdk/typescript/integrations/vercel/vercel)。
