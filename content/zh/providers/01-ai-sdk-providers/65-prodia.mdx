---
title: Prodia
description: 了解如何将 Prodia 模型与 AI SDK 结合使用。
---

# Prodia 提供商

[Prodia](https://prodia.com/) 是一个面向生成式 AI 的快速推理平台，提供基于 FLUX 和 Stable Diffusion 模型的高速图像生成功能。

## 安装

Prodia 提供商可通过 `@ai-sdk/prodia` 模块获取。你可以使用以下命令安装：

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab>
    <Snippet text="pnpm add @ai-sdk/prodia" dark />
  </Tab>
  <Tab>
    <Snippet text="npm install @ai-sdk/prodia" dark />
  </Tab>
  <Tab>
    <Snippet text="yarn add @ai-sdk/prodia" dark />
  </Tab>

  <Tab>
    <Snippet text="bun add @ai-sdk/prodia" dark />
  </Tab>
</Tabs>

## 提供商实例

你可以从 `@ai-sdk/prodia` 导入默认的提供商实例 `prodia`：

```ts
import { prodia } from '@ai-sdk/prodia';
```

如果需要自定义配置，可以导入 `createProdia` 并使用你的设置创建提供商实例：

```ts
import { createProdia } from '@ai-sdk/prodia';

const prodia = createProdia({
  apiKey: 'your-api-key', // 可选，默认使用 PRODIA_TOKEN 环境变量
  baseURL: 'custom-url', // 可选
  headers: {
    /* 自定义请求头 */
  }, // 可选
});
```

你可以使用以下可选设置来自定义 Prodia 提供商实例：

- **baseURL** _string_

  为 API 调用使用不同的 URL 前缀。
  默认前缀为 `https://inference.prodia.com/v2`。

- **apiKey** _string_

  通过 `Authorization` 请求头以 Bearer 令牌形式发送的 API 密钥。
  默认使用 `PRODIA_TOKEN` 环境变量。

- **headers** _Record&lt;string,string&gt;_

  要包含在请求中的自定义请求头。

- **fetch** _(input: RequestInfo, init?: RequestInit) => Promise&lt;Response&gt;_

  自定义 [fetch](https://developer.mozilla.org/en-US/docs/Web/API/fetch) 实现。
  你可以将其用作拦截请求的中间件，
  或为测试等场景提供自定义 fetch 实现。

## 图像模型

你可以使用 `.image()` 工厂方法创建 Prodia 图像模型。
有关使用 AI SDK 生成图像的更多信息，请参阅 [generateImage()](/docs/reference/ai-sdk-core/generate-image)。

### 基本用法

```ts
import { writeFileSync } from 'node:fs';
import { prodia } from '@ai-sdk/prodia';
import { generateImage } from 'ai';

const { image } = await generateImage({
  model: prodia.image('inference.flux-fast.schnell.txt2img.v2'),
  prompt: '一只穿着精致长袍的猫',
});

const filename = `image-${Date.now()}.png`;
writeFileSync(filename, image.uint8Array);
console.log(`图像已保存至 ${filename}`);
```

### 模型能力

Prodia 为各种图像生成模型提供快速推理。以下是支持的模型类型：

| 模型                                       | 描述                       |
| ------------------------------------------ | -------------------------- |
| `inference.flux-fast.schnell.txt2img.v2`   | 用于文生图的快速 FLUX Schnell 模型 |
| `inference.flux.schnell.txt2img.v2`        | 用于文生图的 FLUX Schnell 模型     |

<Note>
  Prodia 支持更多模型 ID。请查阅 [Prodia
  文档](https://docs.prodia.com/) 获取完整可用模型列表。
</Note>

### 图像尺寸

你可以使用 `size` 参数以 `WIDTHxHEIGHT` 格式指定图像尺寸：

```ts
import { prodia } from '@ai-sdk/prodia';
import { generateImage } from 'ai';

const { image } = await generateImage({
  model: prodia.image('inference.flux-fast.schnell.txt2img.v2'),
  prompt: '日落时分宁静的山景',
  size: '1024x768',
});
```

### 提供商选项

Prodia 图像模型通过 `providerOptions.prodia` 对象支持额外选项：

```ts
import { prodia, type ProdiaImageModelOptions } from '@ai-sdk/prodia';
import { generateImage } from 'ai';

const { image } = await generateImage({
  model: prodia.image('inference.flux-fast.schnell.txt2img.v2'),
  prompt: '一只穿着精致长袍的猫',
  providerOptions: {
    prodia: {
      width: 1024,
      height: 768,
      steps: 4,
      stylePreset: 'cinematic',
    } satisfies ProdiaImageModelOptions,
  },
});
```

支持以下提供商选项：

- **width** _number_ - 输出宽度（像素，256–1920）。设置后将覆盖从 `size` 派生的宽度。
- **height** _number_ - 输出高度（像素，256–1920）。设置后将覆盖从 `size` 派生的高度。
- **steps** _number_ - 计算迭代次数（1–4）。更多步骤通常产生更高质量的结果。
- **stylePreset** _string_ - 为输出图像应用视觉主题。支持的预设：`3d-model`、`analog-film`、`anime`、`cinematic`、`comic-book`、`digital-art`、`enhance`、`fantasy-art`、`isometric`、`line-art`、`low-poly`、`neon-punk`、`origami`、`photographic`、`pixel-art`、`texture`、`craft-clay`。
- **loras** _string[]_ - 使用最多 3 个 LoRA 模型增强输出。
- **progressive** _boolean_ - 使用 JPEG 输出时，返回渐进式 JPEG。

### 种子

你可以使用 `seed` 参数获得可重现的结果：

```ts
import { prodia } from '@ai-sdk/prodia';
import { generateImage } from 'ai';

const { image } = await generateImage({
  model: prodia.image('inference.flux-fast.schnell.txt2img.v2'),
  prompt: '日落时分宁静的山景',
  seed: 12345,
});
```

### 提供商元数据

`generateImage` 响应在 `providerMetadata.prodia.images[]` 中包含提供商特定的元数据。每个图像对象可能包含以下属性：

- **jobId** _string_ - 生成任务的唯一标识符。
- **seed** _number_ - 生成时使用的种子。有助于重现结果。
- **elapsed** _number_ - 生成时间（秒）。
- **iterationsPerSecond** _number_ - 处理速度指标。
- **createdAt** _string_ - 任务创建时间戳。
- **updatedAt** _string_ - 任务最后更新时间戳。

```ts
import { prodia } from '@ai-sdk/prodia';
import { generateImage } from 'ai';

const { image, providerMetadata } = await generateImage({
  model: prodia.image('inference.flux-fast.schnell.txt2img.v2'),
  prompt: '日落时分宁静的山景',
});

// 访问提供商元数据
const metadata = providerMetadata?.prodia?.images?.[0];
console.log('任务 ID:', metadata?.jobId);
console.log('种子:', metadata?.seed);
console.log('耗时:', metadata?.elapsed);
```
