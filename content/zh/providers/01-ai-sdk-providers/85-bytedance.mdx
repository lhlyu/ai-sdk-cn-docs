---
title: "ByteDance"
description: "了解如何将 ByteDance Seedance 视频和 Seedream 图像模型与 AI SDK 结合使用。"
---

# ByteDance 提供商

[ByteDance](https://www.bytedance.com/) 提供商通过 [BytePlus ModelArk](https://docs.byteplus.com/en/docs/ModelArk/) 平台支持 Seedance 系列视频生成模型和 Seedream 系列图像生成模型。Seedance 提供高质量的文本转视频和图像转视频生成能力，包括音视频同步、首尾帧控制和多参考图像生成（参见[视频生成 API](https://docs.byteplus.com/en/docs/ModelArk/Video_Generation_API)）。Seedream 提供文本转图像和图像转图像生成，包括多图像融合和批量图像生成（参见[图像生成 API](https://docs.byteplus.com/en/docs/ModelArk/1824690)）。

## 设置

ByteDance 提供商可通过 `@ai-sdk/bytedance` 模块获取。您可以通过以下命令安装：

<InstallPackages packages="@ai-sdk/bytedance" />

## 提供商实例

您可以从 `@ai-sdk/bytedance` 导入默认提供商实例 `byteDance`：

```ts
import { byteDance } from '@ai-sdk/bytedance';
```

如果您需要自定义设置，可以导入 `createByteDance` 并使用您的配置创建提供商实例：

```ts
import { createByteDance } from '@ai-sdk/bytedance';

const byteDance = createByteDance({
  apiKey: 'your-api-key', // 可选，默认为 ARK_API_KEY 环境变量
  baseURL: 'custom-url', // 可选
  headers: {
    /* 自定义请求头 */
  }, // 可选
});
```

您可以使用以下可选设置来自定义 ByteDance 提供商实例：

- **baseURL** _string_

  为 API 调用使用不同的 URL 前缀，例如使用代理服务器。
  默认前缀为 `https://ark.ap-southeast.bytepluses.com/api/v3`。

- **apiKey** _string_

  通过 `Authorization` 请求头发送的 API 密钥。
  默认为 `ARK_API_KEY` 环境变量。
  您可以从 BytePlus 控制台[获取 API 密钥](https://console.byteplus.com/ark/apiKey)。

- **headers** _Record&lt;string,string&gt;_

  要包含在请求中的自定义请求头。

- **fetch** _(input: RequestInfo, init?: RequestInit) => Promise&lt;Response&gt;_

  自定义 [fetch](https://developer.mozilla.org/en-US/docs/Web/API/fetch) 实现。
  您可以将其用作中间件来拦截请求，
  或提供自定义 fetch 实现，例如用于测试。

## 图像模型

您可以使用 `.image()` 工厂方法创建 ByteDance Seedream 图像模型。
有关使用 AI SDK 进行图像生成的更多信息，请参阅 [generateImage()](/docs/reference/ai-sdk-core/generate-image)。

### 文本转图像

从文本提示词生成图像：

```ts
import { byteDance, type ByteDanceImageModelOptions } from '@ai-sdk/bytedance';
import { generateImage } from 'ai';

const { image } = await generateImage({
  model: byteDance.image('seedream-5-0-260128'),
  prompt: 'A salamander in a forest pond at dusk surrounded by fireflies',
  size: '2048x2048',
  providerOptions: {
    bytedance: {
      watermark: false,
    } satisfies ByteDanceImageModelOptions,
  },
});
```

<Note>
  ByteDance 通过其 ModelArk 模型 ID（例如 `seedream-5-0-260128`）或特定于账户的端点 ID（例如 `ep-...`）来标识 Seedream 模型。`size` 参数接受像素尺寸（`{width}x{height}`）；诸如 `2K` 之类的分辨率级别可以通过 `providerOptions.bytedance.size` 传递。
</Note>

### 图像编辑

通过 `prompt.images` 传递输入图像来转换现有图像（图像转图像）：

```ts
import { readFileSync } from 'node:fs';
import { byteDance, type ByteDanceImageModelOptions } from '@ai-sdk/bytedance';
import { generateImage } from 'ai';

const inputImage = readFileSync('./input-image.png');

const { image } = await generateImage({
  model: byteDance.image('seedream-5-0-260128'),
  prompt: {
    text: 'Change the salamander to a snow weasel',
    images: [inputImage],
  },
  providerOptions: {
    bytedance: {
      watermark: false,
    } satisfies ByteDanceImageModelOptions,
  },
});
```

您还可以传递多张图像，将多个参考图像的风格和元素融合到单个输出中（多图像融合）。图像可以以二进制数据、base64 字符串或 URL 的形式提供：

```ts
import { byteDance, type ByteDanceImageModelOptions } from '@ai-sdk/bytedance';
import { generateImage } from 'ai';

const { image } = await generateImage({
  model: byteDance.image('seedream-5-0-260128'),
  prompt: {
    text: 'Replace the clothing in image 1 with the outfit from image 2',
    images: ['https://example.com/model.png', 'https://example.com/outfit.png'],
  },
  providerOptions: {
    bytedance: {
      watermark: false,
    } satisfies ByteDanceImageModelOptions,
  },
});
```

<Note>
  Seedream 不支持基于蒙版的修复。请在提示词中描述编辑内容，可以选择在输入图像上绘制标记（交互式编辑，由 `dola-seedream-5-0-pro-260628` 支持）。
</Note>

### 图像模型选项

以下选项可通过 `providerOptions.bytedance` 使用。您可以使用 `ByteDanceImageModelOptions` 对它们进行类型标注。

- **watermark** _boolean_

  是否在输出图像的右下角添加“AI 生成”水印。

- **outputFormat** _'png' | 'jpeg'_

  生成图像文件的格式。由 `seedream-5-0` 和 `dola-seedream-5-0-pro` 支持；`seedream-4-5` / `seedream-4-0` 始终返回 `jpeg`。

- **size** _string_

  分辨率级别（例如 `1K`、`2K`、`3K`、`4K`），作为通过顶层 `size` 参数传递像素尺寸的替代方案。设置后，此选项将覆盖顶层 `size`。可用级别因模型而异。

- **sequentialImageGeneration** _'auto' | 'disabled'_

  设置为 `'auto'` 以生成一批相关图像（例如故事板或品牌视觉素材）。默认为 `'disabled'`（单张图像）。

- **maxImages** _number_

  当 `sequentialImageGeneration` 为 `'auto'` 时，要生成的最大图像数量。输入参考图像数量加上生成图像数量不得超过模型的限制。

- **optimizePromptMode** _'standard' | 'fast'_

  提示词优化模式。`seedream-4-0` 支持 `standard` 和 `fast` 两种模式；其他模型仅支持 `standard`。

<Note>
  其他 ModelArk 字段也可以通过 `providerOptions.bytedance` 传递，并由 ModelArk 进行验证。
</Note>

### 图像模型能力

| 模型                | 模型 ID                                                  | 能力                                                                                                                                   |
| ------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Seedream 5.0 Pro    | `dola-seedream-5-0-pro-260628`                           | 文本转图像、单/多图像转图像、交互式编辑（标记）。尺寸：1K、2K。格式：png、jpeg。最多 10 个参考。                                        |
| Seedream 5.0 Lite   | `seedream-5-0-260128`（别名 `seedream-5-0-lite-260128`） | 文本转图像、单/多图像转图像、批量生成。尺寸：2K、3K、4K。格式：png、jpeg。最多 14 个参考。                                              |
| Seedream 4.5        | `seedream-4-5-251128`                                    | 文本转图像、单/多图像转图像、批量生成。尺寸：2K、4K。格式：jpeg。最多 14 个参考。                                                       |
| Seedream 4.0        | `seedream-4-0-250828`                                    | 文本转图像、单/多图像转图像、批量生成、快速提示词模式。尺寸：1K、2K、4K。格式：jpeg。最多 14 个参考。                                  |

<Note>
  如有需要，您也可以传递任何模型或端点 ID 字符串，例如用于此处尚未列出的未来模型。目前，AI SDK 集成不支持流式图像输出。
</Note>

## 视频模型

您可以使用 `.video()` 工厂方法创建 ByteDance 视频模型。
有关使用 AI SDK 进行视频生成的更多信息，请参阅 [generateVideo()](/docs/reference/ai-sdk-core/generate-video)。

### 文本转视频

从文本提示词生成视频：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('seedance-1-0-pro-250528'),
  prompt:
    'Photorealistic style: Under a clear blue sky, a vast expanse of white daisy fields stretches out. The camera gradually zooms in and fixates on a close-up of a single daisy.',
  aspectRatio: '16:9',
  duration: 5,
  providerOptions: {
    bytedance: {
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});

console.log(video.url);
```

### 图像转视频

从首帧图像和可选的文本提示词生成视频：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('seedance-1-5-pro-251215'),
  prompt: {
    image: 'https://example.com/first-frame.png',
    text: 'The cat slowly turns its head and blinks',
  },
  duration: 5,
  providerOptions: {
    bytedance: {
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});
```

<Note>
  当输出比例由输入决定时——首帧或首尾帧图像转视频、视频编辑和视频扩展——请传递 `aspectRatio: 'adaptive'` 或完全省略 `aspectRatio`。较新的 Seedance 模型在这些路径上会拒绝显式比例，因为输出会继承输入媒体的比例。
</Note>

### 带音频的图像转视频

Seedance 1.5 Pro 支持在生成视频的同时生成同步音频：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('seedance-1-5-pro-251215'),
  prompt: {
    image: 'https://example.com/pianist.png',
    text: 'A young man sits at a piano, playing calmly. Gentle piano music plays in sync with his movements.',
  },
  duration: 5,
  providerOptions: {
    bytedance: {
      generateAudio: true,
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});
```

### 首尾帧视频

在起始和结束关键帧图像之间生成平滑过渡：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('seedance-1-5-pro-251215'),
  prompt: {
    image: 'https://example.com/first-frame.jpg',
    text: 'Create a 360-degree orbiting camera shot based on this photo',
  },
  duration: 5,
  providerOptions: {
    bytedance: {
      lastFrameImage: 'https://example.com/last-frame.jpg',
      generateAudio: true,
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});
```

### 多参考图像转视频

使用 Seedance 1.0 Lite I2V 模型，您可以提供多个参考图像（1-4 张），模型将使用这些图像忠实地再现物体的形状、颜色和纹理：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('seedance-1-0-lite-i2v-250428'),
  prompt:
    'A boy wearing glasses and a blue T-shirt from [Image 1] and a corgi dog from [Image 2], sitting on the lawn from [Image 3], in 3D cartoon style',
  aspectRatio: '16:9',
  duration: 5,
  providerOptions: {
    bytedance: {
      referenceImages: [
        'https://example.com/boy.png',
        'https://example.com/corgi.png',
        'https://example.com/lawn.png',
      ],
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});
```

### 参考视频

Seedance 2.0 支持参考视频，用于指导生成视频的风格、运动或构图：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('dreamina-seedance-2-0-260128'),
  prompt:
    'First-person perspective promotional ad, using the composition and camera movement from the reference video',
  aspectRatio: '16:9',
  duration: 4,
  providerOptions: {
    bytedance: {
      referenceVideos: ['https://example.com/reference-video.mp4'],
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});
```

### 参考音频

Seedance 2.0 支持参考音频，用作生成视频的背景音乐或声音：

```ts
import { byteDance, type ByteDanceVideoModelOptions } from '@ai-sdk/bytedance';
import { experimental_generateVideo as generateVideo } from 'ai';

const { video } = await generateVideo({
  model: byteDance.video('dreamina-seedance-2-0-260128'),
  prompt: 'A serene mountain landscape at sunrise with gentle camera movement',
  aspectRatio: '16:9',
  duration: 4,
  providerOptions: {
    bytedance: {
      referenceAudio: ['https://example.com/background-music.mp3'],
      generateAudio: true,
      watermark: false,
    } satisfies ByteDanceVideoModelOptions,
  },
});
```

### 视频模型选项

以下选项可通过 `providerOptions.bytedance` 使用。您可以使用 `ByteDanceVideoModelOptions` 对它们进行类型标注。

#### 生成选项

- **watermark** _boolean_

  是否在生成的视频中添加水印。

- **generateAudio** _boolean_

  是否为视频生成同步音频。由 Seedance 1.5 Pro 和 Seedance 2.0 支持。

- **cameraFixed** _boolean_

  是否在生成过程中固定相机。

- **returnLastFrame** _boolean_

  是否返回生成视频的最后一帧。对于串联连续视频很有用。

- **serviceTier** _'default' | 'flex'_

  推理层级。`'default'` 用于在线推理。`'flex'` 用于离线推理，价格为 50%，但延迟更高（响应时间以小时计）。

- **draft** _boolean_

  启用草稿采样模式以进行低成本预览生成。仅由 Seedance 1.5 Pro 支持。生成 480p 预览视频，以便在提交全质量生成之前进行快速迭代。

#### 图像输入选项

- **lastFrameImage** _string_

  用于首尾帧视频生成的最后一帧图像的 URL。模型在首帧（通过 `image` 提示词提供）和此最后一帧之间生成平滑过渡。由 Seedance 1.5 Pro、1.0 Pro 和 1.0 Lite I2V 支持。

- **referenceImages** _string[]_

  用于多参考图像转视频生成的参考图像 URL 数组。模型从每个图像中提取关键特征并在视频中再现它们。在提示词中使用 `[Image 1]`、`[Image 2]` 等来引用特定图像。

#### 媒体参考选项

- **referenceVideos** _string[]_

  用于参考引导视频生成的参考视频 URL 数组（最多 3 个视频，每个最长 15 秒）。模型使用参考视频来指导风格、运动或构图。由 Seedance 2.0 支持。

- **referenceAudio** _string[]_

  用于音频引导视频生成的参考音频 URL 数组（最多 3 个，每个最长 15 秒）。模型将参考音频用作背景音乐或同步声音。支持数据 URI（例如 `data:audio/wav;base64,...`）。由 Seedance 2.0 支持。

#### 轮询选项

ByteDance 视频生成是基于任务的：提供商创建一个任务，AI SDK 轮询该任务直到完成。使用 [`generateVideo()`](/docs/reference/ai-sdk-core/generate-video) 的顶层 `poll` 选项配置轮询：

```ts
const { video } = await generateVideo({
  model: byteDance.video('seedance-1-0-pro-250528'),
  prompt: 'A futuristic city with flying cars',
  poll: {
    intervalMs: 2000, // 检查任务的频率（默认：5000）
    timeoutMs: 900000, // 15 分钟后放弃（默认：600000）
  },
});
```

<Note>
  `pollIntervalMs` 和 `pollTimeoutMs` 提供商选项已弃用并被忽略。传递任一选项都会发出警告。视频生成可能需要几分钟，因此对于长时间任务，请将 `poll.timeoutMs` 提高到 10 分钟默认值以上。
</Note>

### 视频模型能力

| 模型                      | 模型 ID                               | 能力                                                                                                                                          |
| ------------------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Seedance 2.0              | `dreamina-seedance-2-0-260128`        | T2V、I2V、参考视频（最多 3 个）、参考音频（最多 3 个）、音视频同步。时长：4-15 秒。分辨率：480p、720p。                                        |
| Seedance 2.0 Fast         | `dreamina-seedance-2-0-fast-260128`   | T2V、I2V、参考视频（最多 3 个）、参考音频（最多 3 个）、音视频同步。针对速度优化。时长：4-15 秒。分辨率：480p、720p。                          |
| Seedance 1.5 Pro          | `seedance-1-5-pro-251215`             | T2V、I2V（首帧）、I2V（首尾帧）、音视频同步、草稿模式。时长：4-12 秒。分辨率：480p、720p、1080p。                                              |
| Seedance 1.0 Pro          | `seedance-1-0-pro-250528`             | T2V、I2V（首帧）、I2V（首尾帧）。时长：2-12 秒。分辨率：480p、720p、1080p。                                                                    |
| Seedance 1.0 Pro Fast     | `seedance-1-0-pro-fast-251015`        | T2V、I2V（首帧）。针对速度和成本优化。时长：2-12 秒。                                                                                          |
| Seedance 1.0 Lite (T2V)   | `seedance-1-0-lite-t2v-250428`        | 仅文本转视频。时长：2-12 秒。分辨率：480p、720p、1080p。                                                                                       |
| Seedance 1.0 Lite (I2V)   | `seedance-1-0-lite-i2v-250428`        | I2V（首帧）、I2V（首尾帧）、多参考图像（1-4 张）。时长：2-12 秒。分辨率：480p、720p。                                                          |

支持的宽高比：`16:9`、`4:3`、`1:1`、`3:4`、`9:16`、`21:9`、`adaptive`（仅限图像转视频）。

所有模型均以 24 fps 输出 MP4 视频。

<Note>
  如有需要，您也可以传递任何模型 ID 字符串，例如用于此处尚未列出的未来模型。
</Note>
